# State

## Current Phase: Phases 42 and 44 both `partial` and blocked on design decisions; geist rev-8 phase re-planning is the next unblocked work
## Status: active — geist rev 8 pivot. Phase 42 (rewind) and Phase 44 (sandbox) are implemented but NOT shippable; see their Residual lists in ROADMAP.md.

## Decisions
- Astro replaces Next.js in all templates and references (Oskar, 2026-02-28)
- Yolo mode — no interactive confirmations needed
- Fork attribution kept in LICENSE
- SST sst.config.ts excluded from tsconfig (types generated at runtime)
- Extension placed in examples/extensions/ following Pi Agent convention
- Phases 5-9: Zvec-based knowledge, GitHub Action CI, Anthropic SDK orchestration, n8n JSON templates, git-based rollback
- Milestone 2 approved: Integration hardening, Phases 19-24 (Oskar, 2026-03-03)
- Milestone 3 approved: Recursive Language Models, Phases 26-30 — inference-time scaffold per Zhang/Kraska/Khattab 2026 (arXiv:2512.24601). Phases 22-25 remain pending backlog, not a blocker. (Oskar, 2026-04-24)
- Milestone 4 approved: geist, Phases 31-40 — a harness-agnostic ACP-client spatial ADE for Quest 3, based on `.planning/specs/geist-spec.md` rev 7 and now subject to `.planning/geist/SECURITY-2026-07-13.md`. The original autonomous loop marked all phases complete from isolated primitives/tests and recorded H-gates as debt. A five-agent audit on 2026-07-13 found the boundary gate, production CLI, Quest client, UI, and cross-layer composition incomplete; Phases 31-39 are reopened and Phase 40 is not started.
- Milestone 7 approved: Unified Distribution, Phases 47-52 — one transactional installer product (`@draht/install` engine, bins `draht-install`/`draht-init`, verbs plan/install/status/doctor/update/uninstall over hash-manifested state in `~/.draht/install/`, data-driven component index) plus a canonical provider-neutral Agent Skills tree at repo-root `skills/` with generated Claude/Codex plugin artifacts replacing hand-mirroring. Adjudicated from the three 2026-08-11 Fable plans (product/skills/updater, all independently re-verified against the codebase before adoption); spec at `.planning/specs/2026-08-12-unified-distribution-product.md`; publish-gated remainder isolated in Phase 52. Geist (31-40), Rewind (41-43), Sandbox (44-46) untouched and still pending. (Oskar via implementation brief, 2026-08-12)
- Milestone 5 planned: Rewind & Checkpoints, Phases 41-43 — first-class /rewind restoring conversation + working-tree state together (Claude Code /rewind / Codex checkpoint parity); conversation branching already exists in coding-agent, file restore today is only the unaudited example extension git-checkpoint.ts. Planned 2026-07-12 via Fable-model architect subagent; spec at .planning/specs/2026-07-12-rewind-checkpoint-design.md. (2026-07-12)
- Milestone 4 pivot: geist absorbs `packages/gateway` and becomes remote control over all draht sessions running on a connected machine, Tailscale as primary connector. Desktop, mobile and spatial (Quest) are three renderers over one core — same building blocks, laid out on a screen or placed in space. Quest hardware H-gates leave the critical path. Spec: `.planning/specs/2026-08-18-geist-remote-control-rev8.md` (Oskar, 2026-08-18)

## Pending Geist Phases
- Phase 31: Foundation — layout retained; privileged-shim boundary loophole and mutation regression pending
- Phase 32: M0 — partial ray/math scaffold; real Spatial SDK panel/input/H0 pending
- Phase 33: M1 — isolated pairing/ASR helpers; production bridge↔Quest voice wire/H1 pending
- Phase 34: M2 — pure context composition; picker/crop/dispatch/H2 pending
- Phase 35: M3 — ACP backend spine retained; product composition, permission UX, security fixes, and H3 pending
- Phase 36: M4 — grammar/registry retained; real command/mode/spawn UX and H4 pending
- Phase 37: M5 — fleet domain retained; board/cards/live fleet/H5 pending
- Phase 38: M6 — variant domain retained; worktree fan-out/compare UI/H6 pending
- Phase 39: M7 — lane transforms retained; live event transport/rendering/H7 pending
- Phase 40: M8 — not started; implementation and H8 pending

## Completed Phases
- Phase 30: Evaluation, Observability & Docs (1 plan, verified 2026-07-12)
- Phase 29: Draht Agent & CLI Integration (1 plan, verified 2026-07-12)
- Phase 28: REPL Sandbox & Safety (1 plan, verified 2026-07-12)
- Phase 27: Sub-LLM Integration & System Prompts (1 plan, verified 2026-07-12)
- Phase 26: RLM Core Primitives (1 plan, verified 2026-07-11)
- Phase 25: CI & Artifact Cleanup (1 plan, verified 2026-07-11)
- Phase 24: Invoice/Compliance Tests (1 plan, verified 2026-07-11)
- Phase 23: Multi-Agent Layer (1 plan, verified 2026-07-11)
- Phase 22: Router Hardening (3 plans, verified 2026-03-16)
- Phase 21: GSD Integration Tests (complete)
- Phase 20: TDD/DDD Hook Hardening (complete)
- Phase 19: GSD CLI Integration (complete)
- Phase 1: Rebrand (7 commits)
- Phase 2: SST v4 Infrastructure (1 commit)
- Phase 3: SST Resource Manager Extension (1 commit)
- Phase 4: AGENTS.md Template Library (1 commit)
- Phase 5: Client Knowledge Base Extension (1 commit)
- Phase 6: CI/CD AI Review Pipeline (1 commit)
- Phase 7: Multi-Agent Orchestration (1 commit)
- Phase 8: n8n Client Workflows (1 commit)
- Phase 9: Deploy Guardian Extension (1 commit)
- Phase 10: Draht CLI & Branding (complete)
- Phase 11: Testing Infrastructure (complete)
- Phase 12: Documentation & README (complete)
- Phase 13: Model Router (3 commits)
- Phase 14: TDD-First Core (2 commits)
- Phase 15: DDD-First Core (2 commits)
- Phase 16: Invoice Generator (2 commits)
- Phase 17: Compliance Checker (2 commits)
- Phase 18: draht.dev Website Content (2 commits)

## Lessons
- 2026-03-02: GSD tracking drifted silently — phase status and plan files went out of sync until a manual audit; keep STATE/ROADMAP updates in the same commit as the work they describe.
- 2026-08-18: The 2026-03-02 lesson repeated at milestone scale — six phases carried a wrong status because work landed in sessions that never touched `.planning/`. "Update in the same commit" is not enforceable by intent; drift must be *detectable*. Add a `check:planning-sync` gate (ROADMAP `**Surface:**` globs per phase, cross-checked against commits since STATE's last activity, phase-dir existence, and tests citing the phase's R-ids) and wire it into `npm run check` AND CI — `check:draht` proves a gate that exists but is never invoked catches nothing.

## Blockers
- Phase 32 requires migration to the current Meta Spatial SDK toolchain and a real panel/input implementation; Meta Spatial Editor is not installed on the development machine.
- Do not start the pairing helper outside tests until GSEC-04 adds a code-enforced loopback bind. After that, development may use loopback plus `adb reverse`; LAN remains blocked until every release-blocking finding in `.planning/geist/SECURITY-2026-07-13.md` is closed.

## Audit Log
- 2026-08-19: Phase 44 (sandbox core) committed as `partial`. All acceptance escape vectors are confined and the self-test gate is sound — it was broken five ways and correctly reported `unavailable` each time rather than running unconfined. But an adversarial escape review found 6 critical escapes rooted in R44-SBX.2's own mandated policy rather than in the implementation: a write allowlist containing the project tree, the OS temp dir and cache roots cannot confine an agent, because each is an execution vector for the unsandboxed shell that runs later. Two rounds of patching closed every named vector and the reviewer found a new class each time. Escalated as a spec decision rather than continuing to patch.
- 2026-08-19: Phase 42 (/rewind) implemented and committed as `partial`, deliberately not `complete`. Its 8 acceptance criteria pass against real fixture repos, but two independent adversarial reviewers blocked it over working-tree data loss: `git add -A` collapses a nested git repository into one gitlink, and both the delete route (`rmSync -r`) and the typechange route (`git checkout-index -f`) destroyed it along with its history and uncommitted work. Both routes are now guarded, verified by reproducing the loss with the guard neutered. Committing it as `partial` with an explicit residual list rather than as `complete` — recording package-level or acceptance-level evidence as product completeness is the exact 2026-07-13 failure mode.
- 2026-08-18: Reconciliation audit — 13 agents verified every pending phase (31, 42-52) against real code and test runs. Six statuses were wrong: Phases 31, 48, 49 were `pending` but are production-e2e complete; Phases 47, 50, 51 are substantially complete and now `partial` with explicit residual lines. Phases 42-46 and 52 confirmed genuinely not started. Root cause: 50 commits landed after 2026-07-13 with no `.planning/` update, and no phase directories exist for 42-52. Verified separately: draht's attach substrate (`packages/coding-agent/src/core/socket-server/`) round-trips 9/9 but has zero call sites — remote control of running sessions is a wiring job, not a build job.
- 2026-07-13: Milestone 4 reality audit completed with five parallel agents covering Quest, bridge/protocol/voice, UI/picker, automated evidence, and core/ACP/security, followed by three-agent consistency/security review. Package-level primitives and tests had been misclassified as product e2e: the boundary gate permits privileged-shim imports, `geist` is a stub, Quest has no Spatial SDK/client flow, the console is a wordmark, protocol mirrors are empty, and M8 has no implementation. Corrected ROADMAP/REQUIREMENTS/STATE/DOMAIN and `.planning/geist/README.md`; Phases 31-39 are reopened as pending, Phase 40 is pending/not started. Reports: `.planning/geist/AUDIT-2026-07-13.md` and `.planning/geist/SECURITY-2026-07-13.md`.
- 2026-07-12: Milestone 6 (Bash Sandbox Confinement) planned in-session (Fable 5), directly following same-day permission-gate work in `packages/coding-agent` (session permission modes default/auto/yolo with `/yolo`+`/permissions` commands and a heuristic danger filter, then inline-interpreter-eval patterns + xargs unwrapping — commits on main). Added Phases 44-46 to ROADMAP (all `pending`), requirement blocks R44-SBX/R45-SBM/R46-SBH to REQUIREMENTS.md, and design spec `.planning/specs/2026-07-12-bash-sandbox-confinement.md`. Rationale: the permission gate's own module doc records interpreter escape hatches as unfixable with text matching — `python -c "os.system('rm -rf /')"` is invisible to shell-shaped patterns (deny rules included), script files hide the danger in file contents the gate never reads, and `shutil.rmtree('/')` involves no shell command at all — so the hard boundary must be OS-level sandboxing of the bash tool itself (macOS Seatbelt, Linux Landlock/namespaces), generalizing Phase 28's proven RLM-REPL pattern (`packages/rlm/src/sandbox.ts`) onto the existing pluggable `BashOperations.exec` seam in `src/core/tools/bash.ts`. Key posture difference from Phase 28: bash is the agent's primary limb, so sandbox-unavailable degrades to today's gate with a notice (never fail-closed), and in-sandbox policy denials escalate to a single "rerun unsandboxed?" approval. No phase executed.
- 2026-07-12: Milestone 5 (Rewind & Checkpoints) planned via Fable-model architect subagent. Added Phases 41-43 to ROADMAP (all `pending`), requirement blocks R41-CKP/R42-RWD/R43-SFT to REQUIREMENTS.md, and design spec `.planning/specs/2026-07-12-rewind-checkpoint-design.md`. Rationale: native rewind — jumping a session back to an earlier point with both conversation and file state restored atomically — to match Claude Code's /rewind and Codex's checkpoint/rewind; the session tree, branch/fork/tree navigation, and hooks already exist in `packages/coding-agent`, but file-state restore exists only as the opt-in, unaudited example `examples/extensions/git-checkpoint.ts` (stash-based: misses untracked files, GC-able dangling commits, in-memory checkpoint map cleared per turn, `stash apply` merges instead of restoring). Design: per-turn git snapshots via temp index anchored at `refs/draht/checkpoints/<session-id>/<entry-id>`, sidecar metadata JSONL, mandatory pre-rewind safety snapshot, diff-driven restore, wired as an always-loaded core builtin. No phase executed; Phase 30 remains the current/last-completed phase in the RLM line (superseded by Milestone 4/geist below, completed in parallel in a separate worktree).
- 2026-07-12: Post-Milestone-3 build regression found and fixed: `bun dev:link` was broken (`packages/coding-agent`'s own build failing with TS5055 collisions across nearly its whole `dist/` tree, and the resulting `draht` binary crashing at startup with `ERR_MODULE_NOT_FOUND` for `packages/rlm/src/loaders.js`). Root cause traced via `tsc --explainFiles`: Phase 29's `rlm-cli.ts` statically imports `@draht/rlm`, which shipped raw source only (`main: "./src/index.ts"`); its `loaders.ts` imports `@draht/knowledge` (also raw-source-only); `knowledge`'s root barrel re-exported its `extension.ts`, which imports `@draht/coding-agent`'s types — closing a compile-time cycle back into the package being built, and (separately) meaning plain Node had no compiled JS to load at runtime for either package. Fixed by (1) removing `knowledgeExtension` from `packages/knowledge/src/index.ts`'s barrel (verified zero consumers repo-wide; it remains available via the existing dedicated `@draht/knowledge/extension` subpath, which is how it's actually meant to be loaded — dynamically, via jiti, not statically bundled) and (2) giving `@draht/router`, `@draht/knowledge`, and `@draht/rlm` real emit-producing builds (new `tsconfig.build.json` per package, `package.json` `main`/`types` pointing at `./dist/index.{js,d.ts}`), with `coding-agent`'s `tsconfig.build.json` path-mapped to their dist output, matching the existing `ai`/`agent-core`/`tui` convention. Verified end-to-end: fresh and stale-dist rebuilds both succeed for all four packages; `bun dev:link` completes; `draht --version`/`--help`/`rlm --help` all run (previously crashed); full test suites for router (80/80), knowledge (7/7), rlm (77/77), rlm-agent (11/11), and coding-agent's rlm-cli (16/16) still pass; full monorepo `tsgo --noEmit` clean; the only remaining failures anywhere in `npm run test --workspaces` are pre-existing, unrelated Amazon Bedrock live-API tests in `packages/ai`.
- 2026-07-12: Phase 30 (Evaluation, Observability & Docs) completed and verified — the final phase of Milestone 3. `@draht/rlm` sessions are now measurable, replayable, and documented: every `createRouterBackedSession` run appends a `TrajectoryStepEntry` per REPL step (code, truncated stdout, error, correlated sub-calls, per-step cost) and one terminal `TrajectoryFinalEntry` to `.draht/rlm/<trajectoryId>.jsonl` (`packages/rlm/src/trajectory.ts`), wired in via an additive `trajectoryLogDir` option with no breaking change to `createRouterBackedSession`'s existing shape; sub-call costs are attributed to the correct step via an accumulate-since-last-step-boundary approach (root calls always precede their step's sub-calls in the existing loop), not retroactive timestamp correlation. A new S-NIAH regression suite (`test/s-niah.test.ts`) proves needle recall at 10x (~4M chars) and 100x (~40M chars) a mocked 100,000-token/400,000-character root-model window, against a scripted mock `rootLlm`. A cost-comparison harness (`packages/rlm/src/cost-comparison.ts`) computes RLM's actual trajectory cost vs. a named "first-N-characters-that-fit" truncate-and-single-call baseline, writing `.draht/rlm/cost-comparison-report.json`. `draht rlm replay <trajectory-id>` (extending Phase 29's `rlm-cli.ts`) reconstructs the final answer from the JSONL log alone, with no router/model object ever constructed on that code path — the strongest available proof of zero LLM calls. `packages/rlm/README.md` was rewritten with a "when to prefer RLM" section, a cost-envelope explanation pointing at the comparison harness, an observability/replay section, a worked example showing `/rlm`/`draht rlm --input`/`rlm_query` side by side, and an explicit "AGENTS.md" section recording the decision that no `packages/templates/` per-stack template is the right home for an RLM note (RLM is cross-cutting, not stack-specific) — so this README remains the primary documentation surface, with the reasoning written into the doc itself rather than only in phase history. Full suite: `packages/rlm` 11 files/77 tests, `packages/coding-agent`'s `rlm-cli.test.ts` 1 file/16 tests — 93/93 passing across both; full monorepo `tsgo --noEmit` clean (via `rtk proxy` after the RTK hook mangled the direct invocation, same known artifact as Phases 28-29). Closes R30-EVAL.1 through R30-EVAL.5. **Milestone 3 (Recursive Language Models, Phases 26-30) is now fully complete**: `@draht/rlm` exists as a working inference-time scaffold (root loop + Python REPL, Phase 26), routed through `@draht/router` with tiered system prompts and per-trajectory cost accounting (Phase 27), sandboxed at the OS process boundary with hard resource/cost ceilings (Phase 28), reachable from real agent flows via `/rlm`, the `draht rlm` CLI, and the `rlm_query` tool with file/glob/URL/knowledge-base input loaders (Phase 29), and now measurable, replayable, and documented (Phase 30). The `packages/knowledge`/`packages/ci`/`packages/deploy-guardian` extension-loading finding flagged in Phase 29 remains unfixed and is still recommended as a follow-up for a future phase — unrelated to Milestone 3 and explicitly out of this phase's scope. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-12: Phase 29 (Draht Agent & CLI Integration) completed and verified: `@draht/rlm` (Phases 26-28) is now actually reachable from real agent flows. Added `packages/rlm/src/loaders.ts` (file/glob/URL/`knowledge:<client-slug>` input loaders, re-exported from the package root); new `packages/rlm-agent/` extension package registering a `/rlm <input> <query>` command and an `rlm_query` tool, with its `package.json` given the correct `"pi": { "extensions": [...] }` manifest field (the exact field whose absence made Phase 23's `core/builtins/subagent.ts` dead code); a `draht rlm --input <path|glob|url> --query "..." [--max-cost <n>]` CLI subcommand dispatched from `main.ts` alongside `handlePackageCommand`/`handleConfigCommand`; and a `/execute-phase` prompt update so a GSD plan can declare `rlm: true` and route task input files at/above 200KB through `rlm_query` instead of a direct read. Crucially, `packages/rlm-agent`'s loadability was **empirically proven, not just unit-tested in isolation**: `test/real-session-loading.test.ts` constructs a real session via `@draht/coding-agent`'s actual `createAgentSessionServices`/`createAgentSessionFromServices`, with a `settings.json` local-package-source entry pointing at the real on-disk `packages/rlm-agent` directory (the exact shape `DefaultPackageManager.parseSource()`/`resolveLocalExtensionSource()`/`collectPackageResources()` read for a real `draht install <path> -l`), asserts `rlm_query`/`/rlm` are present in that real session, and includes a negative-control test proving they're absent when the package isn't configured — the same class of proof the Phase 23 fix used, applied proactively this time instead of after the fact. Full suite: `packages/rlm` 8 files/67 tests, `packages/rlm-agent` 2 files/11 tests, `packages/coding-agent`'s `rlm-cli.test.ts` 1 file/8 tests — 86/86 passing across all three; full monorepo `tsgo --noEmit` clean. Closes R29-INT.1 through R29-INT.5. **Flagged, not fixed, and recommended as a follow-up for a future phase**: while scoping this phase, `packages/knowledge` (Phase 5), `packages/ci` (Phase 6), and `packages/deploy-guardian` (Phase 9) were found to show the same missing-`"pi"`-manifest / missing-root-entry-point pattern that caused Phase 23's dead-code bug — none of the three has been confirmed reachable through the real settings-driven package-resolution path, and none was audited or fixed here (explicitly out of scope for this plan, to avoid scope creep). A future phase should audit all three the way Phase 23 and this phase were audited (trace every real call site: `main.ts`, `agent-session.ts`, `agent-session-services.ts`, `sdk.ts`) and add the missing manifest fields plus real-session-loading regression tests if any is confirmed dead. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-12: Phase 28 (REPL Sandbox & Safety) completed and verified: the plan itself was redesigned pre-implementation after a Fable 5 security-advisor consult, moving the real security boundary from a naive in-process `__builtins__`/import-hook design (trivially bypassable via the Python object graph, e.g. `(_ for _ in ()).gi_frame.f_builtins`) to the OS process boundary — macOS `sandbox-exec` with a deny-default SBPL profile (`packages/rlm/sandbox/macos.sb`), Linux `unshare` user/net/mount namespaces, fail-closed if neither can be established. Python-level builtins/import allowlisting and an AST pre-screen were kept as defense-in-depth guardrails, explicitly not the boundary. Added Node-side RSS polling + wall-clock timeout as the real resource limits, an incremental Python-side stdout cap (fixes an OOM-via-print gap), diff-based (not deepcopy) rollback, and pre-dispatch budget checks with typed stop reasons (`sandbox_violation` among them). A post-implementation Fable 5 review returned `needs-fix`: the OS boundary itself was independently confirmed sound, but `test/sandbox.test.ts` was red (6/7 failing) because the tests were routed through the Python guardrail layer instead of the OS layer directly, and the plan's required runtime startup self-test was missing despite a commit message claiming otherwise. Both fixed in commit `9d2397e98` (rewrote the sandbox tests to bypass `repl_driver.py`'s guardrails and hit `spawnSandboxed` directly; added a real `self_test` wire round-trip run immediately after spawn); that commit also stopped leaking the full parent process environment into the sandboxed child. `packages/rlm` vitest: 7 files, 61/61 passing; full monorepo `tsgo --noEmit` clean. The Linux namespace path is implemented per spec but unverified on this macOS dev machine — needs real coverage from `ci.yml`'s `ubuntu-latest` runner on the next push. Closes R28-SBX.1 through R28-SBX.6. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-12: Phase 27 (Sub-LLM Integration & System Prompts) completed and verified: `RlmSession` is now wired to real models through `@draht/router` without touching `session.ts`'s core loop. Added `rlm-root` (primary `anthropic/claude-opus-4-6`, fallback `google/gemini-2.5-pro`) and `rlm-sub` (primary `google/gemini-2.5-flash`, fallback `openrouter/deepseek-v3.2`) router roles with independent fallback chains, and an additive `CostEntry.trajectoryId` field. Wrote three tuned system-prompt templates (`frontier`/`coder-mid`/`small-context`) with `{{token}}` substitution for all 5 required vars and a shared batching advisory (avoid one-item-per-call, batch ~10-15k chars); `selectTier` auto-picks from the resolved root model's context window at `>=500_000` / `>=128_000` / else thresholds, both boundaries tested directly. New `createRouterBackedSession` factory builds the `rootLlm`/`llmQuery` callbacks over `router.streamSimple`, and logs a `CostEntry` (via existing `estimateCost`/`logCost`) for every root and sub call, tagged with one `trajectoryId` per session. Closes R27-SLM.1 through R27-SLM.5. `packages/rlm` vitest: 4 files, 30/30 passing; `packages/router`'s vitest run couldn't execute (package uses `bun:test`, not vitest — not installed/hoisted), so its actual `bun test` runner was used instead as real signal: 80/80 passing; full monorepo `tsgo --noEmit` clean. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-11: Phase 26 (RLM Core Primitives) completed and verified: `@draht/rlm` package scaffolded with a working `RlmSession` root loop (root-LLM-produces-code → REPL-executes → truncated-stdout → history-append → FINAL-check), a stdlib-only Python REPL driver with a persistent `exec()` globals dict and newline-delimited JSON wire protocol, `FINAL`/`FINAL_VAR` implemented as real function calls raising a caught driver-internal exception (not text-pattern matching, closing R26-RLM.6's brittleness safeguard), and an injectable `llm_query`/`rootLlm` (real `@draht/router` wiring deferred to Phase 27 as planned). `packages/rlm` vitest suite: 2 files, 15/15 tests passing; full monorepo typecheck clean. Closes R26-RLM.1 through R26-RLM.7. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-11: Phase 25 (CI & Artifact Cleanup) completed and verified: confirmed R25-CI.1 (`.github/workflows/ci.yml`) was already satisfied; added `.github/workflows/ai-review.yml` dogfooding `packages/ci`'s composite action on this repo's own PRs (R25-CI.2); backfilled all 11 placeholder Phase 14-18 summary files with real git-history-backed content, explicitly noting the two Phase 14 tasks with no in-repo commit evidence rather than fabricating them (R25-DOC.1); consolidated `hooks.json` to a single generated source of truth via `scripts/generate-hooks-json.mjs` plus a new `check:hooks` step in `npm run check` (R25-DOC.2). **R25-CI.2's `ai-review.yml` workflow is inert until Oskar adds the `ANTHROPIC_API_KEY` repository secret** — `gh secret list` showed none configured, so the `review` job is job-level-gated to skip (not fail) until then. Full `npm run check` passes; ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-11: Phase 24 (Invoice/Compliance Tests) completed and verified: added Lexoffice mock integration tests, Toggl mock integration tests, a realistic German-corpus PII scanner accuracy test, and an EU AI Act documentation-validator test — closing R24-API.1 through R24-API.4. `packages/invoice` 16/16 tests passing across 3 files; `packages/compliance` 18/18 tests passing across 3 files; full monorepo `tsgo --noEmit` clean. No source changes needed (test-coverage phase only). ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-11: Post-verification check on Phase 23 found `core/builtins/subagent.ts` (the module the permission gate was wired into) was never actually loaded by any real session — `discoverAndLoadExtensions` is unused in production and the real settings-driven extension resolution never referenced `core/builtins/`. Traced every call site (cli.ts, main.ts, agent-session.ts, sdk.ts, package-manager.ts) to confirm. Fixed in commit `433e6afbe`: `core/builtins/index.ts` exports `CORE_BUILTIN_EXTENSIONS`, now always included by both `createAgentSessionServices` and `createAgentSession`. Verified empirically via `session.agent.beforeToolCall` on a from-scratch session — a real `.draht/permissions.yml` deny rule now blocks sudo/chaining/command-substitution bypass attempts. Checked Phase 7 (`packages/orchestrator`) for the same class of bug: it's unaffected — that package is a normal opt-in extension (install via `draht install @draht/orchestrator`) working as designed; `core/builtins/` is unrelated, pre-dates the phase system, and just never had a loading mechanism at all.
- 2026-07-11: Phase 23 (Multi-Agent Layer) completed and verified: FSM protocol, teammate mailbox, autonomous task board, worktree isolator, and permission gate all implemented with vitest coverage (6 test files, 70/70 passing) and wired into the subagent builtin. Closes the permission-gate gap (R23-MA.5) identified relative to the Claude Code architecture diagram. ROADMAP.md and STATE.md updated to `complete`.
- 2026-07-11: GSD tracking drift found again (same class of bug as 2026-03-02 lesson): Phase 22 was fully planned, executed, and verified on 2026-03-16 (3 plans, real commits, 71/71 tests passing) but ROADMAP.md still listed it `pending`. Corrected ROADMAP.md and STATE.md to `complete`. Beginning autonomous sweep of Phases 22-30 (Oskar approved full autonomy, direct-to-main, 2026-07-11).
- 2026-03-02: GSD tracking audit performed. All 18 phases verified as code-complete (quality gate passes). Fixed:
  - ROADMAP Phase 13 status corrected from `in-progress` to `complete`
  - Phase 3: Added missing 03-01-PLAN.md and 03-01-SUMMARY.md
  - Phase 4: Created missing phase directory with 04-01-PLAN.md and 04-01-SUMMARY.md
  - Phase 7: Added missing 07-01-SUMMARY.md
  - Phase 8: Added missing 08-01-PLAN.md and 08-01-SUMMARY.md
  - Phase 9: Added missing 09-01-PLAN.md and 09-01-SUMMARY.md
- 2026-03-03: Milestone 2 approved. Phases 19-24 added to ROADMAP. Carried forward incomplete requirements from R14-TDD and R15-DDD.
- 2026-03-16: Phase 21 verification completed. All 31 deliverables passed UAT. 22/22 tests passed. No fix plans required.
- 2026-04-24: Milestone 3 (RLM) planned. Added Phases 26-30 to ROADMAP and R26-R30 requirement blocks. Milestone 2 phases 22-25 deferred to backlog.
- 2026-07-11: Milestone 4 (geist) planned from the locked spec. Added Phases 31-40 to ROADMAP, R31-FOUND through R40-M8 requirement blocks to REQUIREMENTS.md, and a geist bounded context (Tier F) to DOMAIN.md. Advisor (fable-5) sanity-checked the phase mapping and confirmed draht-tools has no sequential phase-completion gating, so a hardware-blocked phase (32/M0) cannot stall 33+.
- 2026-07-11: Phase 31 executed via a 6-agent ultracode Workflow (4 parallel `draht:implementer` on sonnet, 1 opus verifier, 1 opus reviewer). Both the verifier and reviewer independently found the same single blocker — `packages/geist-protocol/test/**/*` wasn't added to root `tsconfig.json`'s `exclude`, unlike the router/invoice/compliance bun:test precedent — fixed with the one-line addition both had already verified. Landed as commit 62b5945a2. All R31-FOUND.1-6 requirements confirmed with quoted command evidence (boundary script genuinely blocks/allows the right imports under injected-violation tests, config schema validates geist.yaml.example, quest/ confirmed not a workspace, full-repo `tsgo --noEmit` clean).
- 2026-07-11: Phase 32 (M0) executed via a 2-agent ultracode Workflow (sonnet implementer, opus reviewer). Panel scaffold + real ray-plane intersection math + both panel-alpha code paths landed as commit 2cc48c9d8; reviewer traced the ray-plane derivation by hand and confirmed no fabricated Spatial SDK API. H0 recorded as evidence debt (needs Oskar's Quest 3) rather than blocking phase completion.
- 2026-07-11: Phase 33 (M1) executed via two ultracode Workflow passes (3 parallel sonnet implementers + opus verify/review, then an opus fix-and-reverify pass). First pass found the same tsconfig-exclude gap as Phase 31 (recurring pattern — new packages need explicit root `tsconfig.json` exclude entries; established convention is a per-package `test/` dir, not co-located `*.test.ts`) plus a real bug in the pairing state machine (`disconnect()` could silently reset the 60s grace clock on a stray reconnect attempt, letting a session stay "paired" indefinitely). Fix pass resolved both with red/green proof of the bug fix; independent re-verify confirmed all 9 checks green before commit 60a3339cb. H1 recorded as evidence debt.
- 2026-07-11: Phase 34 (M2) executed via a 3-agent ultracode Workflow (sonnet implementer, opus verify, opus review) — clean on the first pass, no fixes needed. ElementContext/situation-prompt composition landed as commit ac3eabe9d; spec §9.3's r2 text wasn't available in this document so the implementer grounded the shape in already-established sources (picker descriptor, HarnessSession id, crop-path convention) instead of guessing — reviewer confirmed this was reasonable, not scope creep.
- 2026-07-12: Advisor (fable-5) consulted again before Phase 35 (M3 — the flagged highest-risk phase): confirmed real, current `@agentclientprotocol/sdk`/`claude-agent-acp`/`codex-acp` npm packages exist (replacing the deprecated `@zed-industries/agent-client-protocol` name), recommended fixing the `HarnessSession.dispatch(prompt: string)` → `dispatch(prompt: SituationPrompt)` seam immediately (done, commit 6963fcc9b) and splitting M3 into 35a (ACP client + mock agent + sha ledger + permission WS relay) / 35b (draht-acp shim + keyless e2e + claude smoke). Phase 35a landed as commit 38b0e7802 after a review pass caught a real security gap (permission WS channel authorized on a global pairing flag, not the specific authenticated socket — a second unauthenticated connection could hijack or answer permission requests) and an unguarded subprocess spawn-failure crash; both fixed and independently re-verified. ACP wire code was cross-checked against the SDK's actual installed `.d.ts` files, not written from training-data memory, since the package is only days old. Phase 35b landed as commit ae22c9f90: draht-acp now really wraps `@draht/coding-agent`'s `AgentSession` with a genuinely-blocking per-tool ACP permission gate (proven via red/green attacks — auto-approve and always-deny both broke the expected test assertions), and the keyless e2e drives Phase 35a's real client against a really-spawned draht-acp subprocess using the existing faux LLM provider — no API key, no network. Phase 35/M3 is complete. This recurring tsconfig-exclude gap (new package test dirs need an explicit root exclude entry) has now hit three phases (31, 33, 35b) — worth remembering as a standing pattern for any future geist phase that adds a new package.
- 2026-07-12: Phase 36 (M4) executed via a 5-agent ultracode Workflow (opus grammar resolver, sonnet registry + ACP commands surface, opus verify + review) — clean on the first pass, no fixes needed. Landed as commit b0307e40e: spec §9.5's exact resolution chain with the "nothing shadows an earlier stage" invariant directly tested (a project named the same as a configured harness resolves as the harness), a three-source project registry (yaml/workspaceRoots/recents) with tested precedence, and live (not snapshotted) ACP advertised-commands surfacing.
- 2026-07-12: Phase 37 (M5) executed via a 4-agent ultracode Workflow (2 parallel sonnet implementers, opus verify + review). Review caught session status never crossing onto the fleet_state wire message even though a fleet board's whole point is knowing which sessions need review, and a docstring that went stale mid-phase — both fixed directly. Landed as commit 4c342e8d9: scoped approve/undo/stop proven isolated across two real, separate git worktrees spanning different projects and harnesses.
- 2026-07-12: Phase 38 (M6) executed via a 4-agent ultracode Workflow (sonnet message schema, opus variant domain logic, opus verify + review) — landed as commit 72e37e38d after one trivial validation-consistency fix. "Winner kept, siblings reset+pruned" proven with mutation-verified tests against real independent git worktrees (skipping either half of sibling teardown breaks a specific assertion).
- 2026-07-12: Phase 39 (M7) hit an account-level Claude session usage limit mid-workflow (3 of 4 agents failed outright) — committed only the one fully-verified, isolated piece that completed (LOOP.md surfacing, commit c034033c6), discarded an unverified partial file from the failed lanes-recognizer agent rather than build on unreviewed code, and rescheduled. On retry after the limit reset, resumed the same workflow run (cached the already-done work, retried the failed agents fresh) and it landed clean: commit 7fd50c58a.
- 2026-07-12 (superseded and retracted by the 2026-07-13 audit): **Milestone 4 (geist) complete — all 10 GSD phases (31-40) done.** Phase 40 (M8) needed no code: its ROADMAP acceptance is entirely hardware-gated with no automated ✅ criterion, so completion was recording H8 as evidence debt. In the process, discovered H2-H7 had been referenced in STATE.md/ROADMAP notes across phases 34-39 but never actually written into `.planning/geist/README.md`'s Evidence debt section — backfilled all of them (H0-H8) with the exact spec §16 wording so the evidence-debt record is now complete and accurate, not just asserted. Final full-repo sanity pass across every geist package (geist, geist-core, geist-acp, geist-protocol, geist-console, draht-acp): root `tsgo --noEmit` clean, `check-geist-boundary`/`check-geist-mirrors` clean, 194 tests passing (1 network-gated skip, 0 failures) across all six packages, every package's own build clean. The geist spec's entire software-buildable scope (every phase's automated ✅ criteria) is implemented, tested, and committed; claimed only the nine H-gates remained, each requiring Oskar's physical Quest 3.
- 2026-07-12: Merged `main` (67 commits ahead — Milestone 3/RLM Phases 22-30 completed, Milestone 5/Rewind-Checkpoints Phases 41-43 planned, in a separate worktree/session while Milestone 4/geist was built here) into this branch to prepare for merge back to main. Real conflicts in `.planning/{STATE,ROADMAP,REQUIREMENTS}.md` and root `package.json`'s `check` script, all reconciled by combining both sides' additions (no content dropped from either branch) rather than picking one side. Found and fixed a milestone-numbering collision main introduced against itself: `REQUIREMENTS.md` labeled the rewind work "Milestone 4" while `STATE.md`'s own decision log already correctly called it "Milestone 5" — since geist's phase numbers (31-40) don't collide with rewind's (41-43), relabeled `REQUIREMENTS.md`'s header to "Milestone 5" to match `STATE.md` and avoid colliding with geist's own real Milestone 4.

## Last Activity: 2026-07-13
