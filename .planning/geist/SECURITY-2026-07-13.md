# Geist security findings — 2026-07-13

**Status:** release-blocking audit baseline. The production `geist` CLI is currently a stub, so most bridge findings become reachable when composition work starts. `draht-acp` findings are already reachable when another ACP client launches it.

A phase may claim a finding closed only when the listed remediation and regression evidence exist. Phase 33 owns pairing transport findings; Phase 35 owns ACP, permissions, process, project trust, and review-state findings.

| ID | Severity | Finding | Owner | Required closure evidence |
|---|---|---|---|---|
| GSEC-01 | Critical | Project-controlled Draht extensions can load as trusted code before ACP authorization | Phase 35 | ACP sessions default to untrusted with every project executable resource disabled; trust may be granted only through an authenticated local bridge control bound to canonical repository/worktree identity, never by a model/headset tool answer; malicious-extension regression proves no load/provider call before trust |
| GSEC-02 | High | Not every active Draht tool path is covered by ACP permission gating | Phase 35 | One final execution hook gates the complete active tool registry, including extension/custom/subagent tools; enumeration and nested-subagent regressions prove every execution reaches ACP authorization |
| GSEC-03 | High | ACP subprocesses inherit the bridge's full environment and unrelated secrets | Phase 35 | Per-harness allowlist-built environment with absolute trusted `PATH`, runtime/locale/temp values, and only that harness's declared auth; arbitrary-name canary tests prove unrelated provider/cloud/CI/SSH secrets do not cross into child or nested processes |
| GSEC-04 | High | Pairing binds all interfaces and uses plaintext reusable bearer authentication | Phase 33 | Server defaults to loopback and reports its actual bind; wildcard bind requires LAN mTLS 1.3 with pinned bridge identity and headset device key; the displayed token is one-time bootstrap only, invalidated after binding; reconnect uses rotated short-lived credentials; replay, revocation, wrong-device, and downgrade regressions pass |
| GSEC-05 | High | Approve/undo does not snapshot or restore the reviewed filesystem state | Phase 35 | A spec amendment defines the managed state and ignored-file policy; the agent is cancelled/quiesced and an exclusive canonical-worktree lock spans action-time review recheck, snapshot, restore, verification, and ledger commit; approval survives restart; edit A → approve → edit B + tracked/untracked/ignored-policy output → undo yields A or an anchored safety state under failure injection |
| GSEC-06 | High | Permission prompts omit command/path/argument detail needed for informed consent | Phase 35 | Protocol carries tool-call ID and canonical cwd/command/path/operation details; bounded rendering preserves decisive suffixes and neutralizes control/bidi spoofing; approval binds to a digest of immutable normalized arguments and execution rejects mutation; browser/headset regressions cover bash and file tools |
| GSEC-07 | High | Git review detection fails open and misses relevant state changes | Phase 35 | Probe failure yields blocking `review_error`; approve performs a locked action-time recheck; exact accepted-state comparison covers ref rollback/divergence, tracked/untracked/ignored policy, concurrent writers, and Git metadata failure |
| GSEC-08 | Medium | An unauthenticated bad `pair` message can invalidate an active pairing | Phase 33 | Pairing attempts are socket-scoped; invalid candidates cannot mutate active state; second-socket/wrong-token regression preserves the legitimate connection |
| GSEC-09 | Medium | ACP lifecycle and NDJSON transport have no bounded cleanup, frame, queue, or output limits | Phase 35 | Numeric deadlines cover spawn/handshake/prompt/cancel/stop; numeric byte/count caps cover NDJSON lines, queued updates/requests, stdout, and stderr; overflow is a typed protocol violation that kills the process tree; TERM→KILL and flood/unterminated-line regressions pass |
| GSEC-10 | Medium | Disconnect drops the permission notification and strands the pending ACP request | Phase 35 | Bounded pending registry replays after authenticated reconnect; expiry fails closed; request is removed on answer/cancel/exit; reconnect/expiry regressions pass |
| GSEC-11 | Low | Permission answers are not validated against the offered option IDs | Phase 35 | Pending state stores immutable offered options; unknown/stale/cross-session IDs are rejected without consuming the valid request; regressions pass |
| GSEC-12 | High | Project/local-path configuration can select project-controlled executable bytes | Phase 33 | Launch specs resolve to canonical absolute executables from a user-owned registry; relative commands/PATH entries and project-local binaries are rejected; config and secure parent are current-UID, non-symlink, race-safely verified, and not group/world accessible; project config may reference only approved harness IDs and canonical roots; malicious-project/PATH/symlink regressions prove no arbitrary launch |
| GSEC-13 | High | Symlinked project context files can exfiltrate arbitrary host files into the model system prompt | Phase 35 | Automatically read project context is a no-follow regular file canonically contained under an approved root; project symlinks/special files and out-of-root ancestor context are rejected; canary regression proves session creation/first provider request never contains out-of-root bytes |

## Immediate posture

- Do not expose `createPairingServer()` on LAN. It currently has no hostname option and therefore cannot enforce the desired loopback posture.
- The production bridge must not accept headset connections until GSEC-04, GSEC-08, and GSEC-12 are closed. Then loopback development may use `adb reverse`; non-loopback operation remains unavailable until GSEC-04's encrypted device-bound LAN protocol is fully implemented.
- Do not advertise `draht-acp` as safely headset-permissioned until GSEC-01 and GSEC-02 are closed.
- Do not rely on approve/undo as confinement until GSEC-05 and GSEC-07 are closed.

## Full-state approval amendment required by GSEC-05

The rev 7 rule `undo = reset --hard <ref>` is insufficient because it neither captures approved uncommitted bytes nor removes untracked/ignored output. Before Phase 35 can close, the canonical spec and public copy must be amended with these decisions:

1. every agent session runs only in a Geist-managed disposable worktree;
2. approval records a durable hidden full-state snapshot/ref, including tracked and untracked non-ignored files;
3. ignored files are explicitly classified as reproducible/disposable or protected, with regressions for both classes;
4. approval/undo cancels and quiesces the agent, then holds an exclusive canonical-worktree lock through action-time review recheck, snapshot, restore, verification, and ledger commit;
5. undo takes an anchored safety snapshot, restores transactionally, and verifies the resulting state;
6. bridge restart reloads `base` and `lastApproved` snapshot identity;
7. failure leaves either the target state or the recoverable safety state, never a silent partial restore.

This audit does not silently revise the locked rev 7 decision. It makes the amendment an explicit prerequisite and security acceptance gate.
