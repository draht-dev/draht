---
phase: 3
plan: 1
depends_on: [2]
must_haves:
  - "SST Resource Manager extension in coding-agent examples"
  - "sst_status tool: reads local .sst/ metadata"
  - "sst_resources tool: parses sst.config.ts for resource definitions"
  - "Never deploys or connects to AWS"
---

# Phase 3, Plan 1: SST Resource Manager Extension

## Tasks

<task type="auto">
  <n>Create SST Resource Manager extension</n>
  <files>packages/coding-agent/examples/extensions/sst-resource-manager.ts</files>
  <action>
    Create extension with two tools:
    - sst_status: Find nearest sst.config.ts, read .sst/ metadata (stage, last activity)
    - sst_resources: Parse sst.config.ts with regex for resource definitions (type, name, line)
    - Walk up directory tree to find SST root
    - Group resources by type in output
  </action>
  <verify>TypeScript compiles, extension follows ExtensionAPI pattern</verify>
  <done>Extension loads in coding-agent, tools registered, no AWS calls</done>
</task>
