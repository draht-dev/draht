---
phase: 8
plan: 1
depends_on: []
must_haves:
  - "n8n workflow JSON templates for onboarding, standup, and invoicing"
  - "Templates importable into n8n"
  - "Package at packages/workflows/"
---

# Phase 8, Plan 1: n8n Client Workflow Templates

## Tasks

<task type="auto">
  <n>Create onboarding workflow template</n>
  <files>packages/workflows/src/onboarding.json</files>
  <action>Create n8n workflow JSON for client onboarding: welcome email, project setup, AGENTS.md generation trigger.</action>
  <verify>Valid JSON, follows n8n workflow schema</verify>
  <done>Onboarding workflow importable into n8n</done>
</task>

<task type="auto">
  <n>Create daily standup workflow template</n>
  <files>packages/workflows/src/daily-standup.json</files>
  <action>Create n8n workflow JSON for daily standup: git log summary, time tracking pull, Slack/email notification.</action>
  <verify>Valid JSON, follows n8n workflow schema</verify>
  <done>Standup workflow importable into n8n</done>
</task>

<task type="auto">
  <n>Create invoicing workflow template</n>
  <files>packages/workflows/src/invoicing.json</files>
  <action>Create n8n workflow JSON for invoicing: time tracking aggregation, invoice generation trigger, Lexoffice integration.</action>
  <verify>Valid JSON, follows n8n workflow schema</verify>
  <done>Invoicing workflow importable into n8n</done>
</task>
