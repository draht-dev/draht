---
phase: 9
plan: 1
depends_on: []
must_haves:
  - "packages/deploy-guardian/ with pre-deploy checks and rollback"
  - "Coding agent extension with deployment safety tools"
  - "Never auto-deploys"
---

# Phase 9, Plan 1: Deploy Guardian Extension

## Tasks

<task type="auto">
  <n>Create deploy guardian package with checks</n>
  <files>packages/deploy-guardian/package.json, packages/deploy-guardian/src/checks.ts, packages/deploy-guardian/src/types.ts</files>
  <action>
    Create packages/deploy-guardian/ with:
    - Pre-deployment checklist (tests pass, no uncommitted changes, branch is clean)
    - Lighthouse integration for performance checks
    - Git-based rollback commands (find last known-good commit)
    - SST-specific deployment safety checks
  </action>
  <verify>TypeScript compiles, checks are comprehensive</verify>
  <done>Pre-deploy checks and rollback automation work</done>
</task>

<task type="auto">
  <n>Create coding agent extension</n>
  <files>packages/deploy-guardian/src/extension.ts, packages/deploy-guardian/src/index.ts</files>
  <action>
    Extension with tools:
    - deploy_check: Run pre-deployment checklist
    - deploy_rollback: Find and suggest rollback to last known-good state
    - Never auto-deploys — always requires human confirmation
  </action>
  <verify>Extension follows ExtensionAPI pattern</verify>
  <done>Extension registered, tools available, no auto-deploy</done>
</task>
