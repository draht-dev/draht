---
phase: 21
plan: 1
depends_on: []
must_haves:
  - "Reusable test utilities for isolated temp git repos"
  - "Stable domain fixture with Order, Customer, OrderItem"
---

# Phase 21, Plan 1: Test Utilities and Fixtures

## Goal
Provide deterministic test support that all other Phase 21 integration plans can reuse.

## Context
- Phase 21 verifies the GSD lifecycle end to end inside `packages/coding-agent`.
- Plans 2-4 depend on isolated git repos with real commits.
- Plan 3 depends on a known fixture codebase whose domain terms are predictable.
- Keep all work inside the GSD test-support boundary.

## Tasks

<task type="auto">
  <n>Specify isolated temp git repo behavior with failing tests</n>
  <context>GSD test support</context>
  <domain>Phase, Plan, Task, CommitResult</domain>
  <files>packages/coding-agent/test/test-utils/git-repo.test.ts</files>
  <test>
    RED phase: Add tests proving a helper can create an isolated temp repo, configure git identity, create an initial commit, and clean up the directory.
    - `createTempGitRepo()` returns a repo path that contains `.git`
    - `git rev-parse HEAD` succeeds after helper setup
    - cleanup removes the temp directory
    - optional helper config is honored without changing observable repo validity
  </test>
  <action>
    GREEN phase: Define the helper contract from the tests first so later integration tests can rely on one reusable repo setup path instead of ad hoc bootstrap code.
  </action>
  <refactor>
    REFACTOR phase: Keep assertions focused on observable git state and remove duplicated shell setup from the test body.
  </refactor>
  <verify>Run the helper test file with Vitest and confirm all assertions pass against a real git repo.</verify>
  <done>Passing tests prove the helper creates a committed temp repo and cleanup removes it.</done>
</task>

<task type="auto">
  <n>Implement the shared git repo helper</n>
  <context>GSD test support</context>
  <domain>Phase, Plan, Task, CommitResult</domain>
  <files>packages/coding-agent/test/test-utils/git-repo.ts</files>
  <test>
    RED phase: Satisfy the failing helper tests without relaxing their expectations.
  </test>
  <action>
    GREEN phase: Implement a typed helper that creates a temp directory, runs `git init`, sets deterministic test user config, writes an initial tracked file, creates the first commit, and exposes cleanup.
  </action>
  <refactor>
    REFACTOR phase: Extract small internal fs/git steps, keep imports top-level, and keep the public helper API minimal and reusable.
  </refactor>
  <verify>Run the helper tests and confirm the returned repo works for downstream GSD integration tests.</verify>
  <done>`createTempGitRepo()` and cleanup behavior are exported and proven by tests.</done>
</task>

<task type="auto">
  <n>Create a stable domain fixture codebase</n>
  <context>GSD test support</context>
  <domain>Order, Customer, OrderItem</domain>
  <files>packages/coding-agent/test/fixtures/domain-fixture/Order.ts, packages/coding-agent/test/fixtures/domain-fixture/Customer.ts, packages/coding-agent/test/fixtures/domain-fixture/OrderItem.ts, packages/coding-agent/test/fixtures/domain-fixture/index.ts, packages/coding-agent/test/gsd-domain-fixture.test.ts</files>
  <test>
    RED phase: Add a fixture-focused test proving the fixture codebase contains predictable exported domain terms.
    - fixture exports or source contain `Order`
    - fixture exports or source contain `Customer`
    - fixture exports or source contain `OrderItem`
    - fixture layout is stable enough for later `mapCodebase` assertions
  </test>
  <action>
    GREEN phase: Create a small TypeScript fixture codebase with explicit exported interfaces/classes and a barrel file so later tests can scan one stable directory.
  </action>
  <refactor>
    REFACTOR phase: Keep naming aligned with ubiquitous language and avoid incidental extra terms that would make extraction assertions noisy.
  </refactor>
  <verify>Run the new fixture test and confirm the expected domain terms are present and stable.</verify>
  <done>Fixture files exist and tests prove `Order`, `Customer`, and `OrderItem` are available for later extraction tests.</done>
</task>

---
Created: 2026-03-12 19:30:28
