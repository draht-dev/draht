---
phase: 21
plan: 1
depends_on: []
must_haves:
  - "Reusable test utilities for creating temp git repos"
  - "Domain fixture files with known entities (Order, Customer, OrderItem)"
---

# Phase 21, Plan 1: Test Utilities and Fixtures

## Goal
Provide foundational test infrastructure for all GSD integration tests.

## Context
- Test isolation: git worktree per test with real temp repos
- Domain fixture must have known entities for map-codebase validation

## Tasks

<task type="auto">
  <n>Create git repo test utilities</n>
  <context>GSD</context>
  <domain>Test utilities for git repository lifecycle management</domain>
  <files>packages/coding-agent/test/test-utils/git-repo.ts</files>
  <test>RED phase: Create test file packages/coding-agent/test/test-utils/git-repo.test.ts with tests for createTempGitRepo() returning { path, cleanup } and verifying .git exists, git config set, initial commit present. Test cleanupRepo() removes directory.</test>
  <action>GREEN phase: Implement createTempGitRepo() using mkdtempSync, execSync for git init/config/commit. Implement cleanupRepo() using rmSync. Follow patterns from gsd-git.test.ts beforeEach/afterEach but extract to reusable functions. Export from git-repo.ts.</action>
  <refactor>REFACTOR phase: Add JSDoc comments. Ensure error handling for cleanup when directory already removed. Add optional config parameter for custom user.email/user.name.</refactor>
  <verify>npm run check passes. Tests in git-repo.test.ts pass. Import works from test files.</verify>
  <done>createTempGitRepo() and cleanupRepo() exported and tested. Can replace inline setup in gsd-git.test.ts (but don't refactor existing tests in this task).</done>
</task>

<task type="auto">
  <n>Create domain fixture files with known entities</n>
  <context>GSD</context>
  <domain>Order (aggregate root), Customer (entity), OrderItem (value object)</domain>
  <files>packages/coding-agent/test/fixtures/domain-fixture/Order.ts, packages/coding-agent/test/fixtures/domain-fixture/Customer.ts, packages/coding-agent/test/fixtures/domain-fixture/OrderItem.ts, packages/coding-agent/test/fixtures/domain-fixture/index.ts</files>
  <test>RED phase: Create packages/coding-agent/test/gsd-domain-fixture.test.ts that uses mapCodebase() on the domain-fixture directory and asserts DOMAIN-HINTS.md contains "Order", "Customer", "OrderItem" as detected types/interfaces.</test>
  <action>GREEN phase: Create TypeScript fixture files with exported interfaces/classes: Order.ts with export interface Order and export class OrderAggregate, Customer.ts with export interface Customer, OrderItem.ts with export interface OrderItem. Create index.ts barrel export. Ensure grep patterns in domain.ts will match these exports.</action>
  <refactor>REFACTOR phase: Add realistic properties to each entity (id, name, quantity, etc.). Add JSDoc comments describing DDD classification (aggregate, entity, value object). Ensure consistent naming conventions.</refactor>
  <verify>npm run check passes. gsd-domain-fixture.test.ts passes. mapCodebase() correctly detects all three domain types in DOMAIN-HINTS.md output.</verify>
  <done>Domain fixture directory exists with Order, Customer, OrderItem files. mapCodebase() can extract type hints from them. Test validates detection.</done>
</task>

<task type="checkpoint:human-verify">
  <n>Verify utilities work for subsequent test plans</n>
  <context>GSD</context>
  <domain>Integration verification</domain>
  <files>packages/coding-agent/test/test-utils/git-repo.ts, packages/coding-agent/test/fixtures/domain-fixture/</files>
  <test>N/A - verification checkpoint</test>
  <action>Run full test suite for new utilities. Demonstrate usage pattern for subsequent plans: import { createTempGitRepo, cleanupRepo } from './test-utils/git-repo.js' in a sample test.</action>
  <refactor>N/A</refactor>
  <verify>Human verifies: (1) git-repo utilities match patterns needed for lifecycle tests, (2) domain fixtures contain sufficient variety for map-codebase validation, (3) exports are accessible from test files.</verify>
  <done>Human confirms utilities are ready for Phase 21 Plans 2-4 to depend on.</done>
</task>

---
Created: 2026-03-11 12:15:51
