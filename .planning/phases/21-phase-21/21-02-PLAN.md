---
phase: 21
plan: 2
depends_on: [1]
must_haves:
  - "Full lifecycle test (create-project → commit-task → verify-phase)"
  - "Real git commits with proper messages"
  - "Execution log entries written"
---

# Phase 21, Plan 2: Full Lifecycle Integration Test

## Goal
Verify the complete GSD workflow works end-to-end with real git operations.

## Context
- R21-INT.1: Full lifecycle test
- Uses createTempGitRepo from Plan 1
- Real git repos, actual commits, verify execution log

## Tasks

<task type="auto">
  <n>Test file setup with real git repo fixtures</n>
  <context>GSD</context>
  <domain>GsdProject, Phase, Plan, Task, ExecutionLog</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>RED phase: Create test file that imports from test-utils/git-repo.js; verify imports resolve and createTempGitRepo/cleanupRepo are callable</test>
  <action>GREEN phase: Create gsd-lifecycle.test.ts with describe block; import { createTempGitRepo, cleanupRepo } from './test-utils/git-repo.js'; import GSD functions from '../src/gsd/index.js'; add beforeEach/afterEach using createTempGitRepo and cleanupRepo; add minimal passing test 'setup works'</action>
  <refactor>Ensure consistent import ordering; add JSDoc describing test scope</refactor>
  <verify>npm run check passes; test file runs with vitest --run test/gsd-lifecycle.test.ts</verify>
  <done>Test file exists with working test-utils imports and one passing skeleton test</done>
</task>

<task type="auto">
  <n>Full lifecycle: create-project to commit-task</n>
  <context>GSD</context>
  <domain>GsdProject, Phase, Plan, Task, CommitResult</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>RED phase: Write test 'full lifecycle: project setup to task commit' that: (1) creates PROJECT.md in .planning/, (2) calls createDomainModel(), (3) calls createPlan(1, 1, 'Test Plan'), (4) writes a test file to repo, (5) calls commitTask(1, 1, 'implement feature'), (6) asserts commit hash exists and message matches feat(01-01): pattern via git log</test>
  <action>GREEN phase: Use existing GSD functions: createDomainModel, createPlan, commitTask; write actual file to temp repo before commit; verify git log output contains expected commit message format</action>
  <refactor>Extract helper for creating minimal PROJECT.md content; add descriptive assertion messages</refactor>
  <verify>Test passes with vitest --run test/gsd-lifecycle.test.ts; git log in temp repo shows correct commit format</verify>
  <done>Integration test verifies create-project → createPlan → commitTask workflow with real git commits</done>
</task>

<task type="auto">
  <n>Full lifecycle: plans to verify-phase</n>
  <context>GSD</context>
  <domain>Phase, Plan, Summary, PhaseVerification</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>RED phase: Write test 'full lifecycle: multiple plans to verify-phase' that: (1) sets up ROADMAP.md with phase entry, (2) creates 2 plans via createPlan, (3) writes summaries via writeSummary for both, (4) calls verifyPhase, (5) asserts complete=true, (6) asserts VERIFICATION.md file exists in phase directory</test>
  <action>GREEN phase: Create ROADMAP.md with '## Phase 1: Test — `pending`' format; call createPlan twice; call writeSummary twice; call verifyPhase and assert result.complete===true; verify VERIFICATION.md exists via fs.existsSync</action>
  <refactor>Use constants for phase/plan numbers; add assertion for plans count and summaries count</refactor>
  <verify>Test passes; phase directory contains PLAN.md, SUMMARY.md, and VERIFICATION.md files</verify>
  <done>Integration test verifies multi-plan phase completion workflow with verification file creation</done>
</task>

<task type="auto">
  <n>Verify execution log entries are written</n>
  <context>GSD</context>
  <domain>ExecutionLog, Task, CommitResult</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>RED phase: Write test 'execution log entries written for task commits' that: (1) creates execution-log.jsonl path in .planning/, (2) appends entry manually matching draht-post-task.js format: {timestamp, phase, plan, task, status, commit}, (3) reads back file and parses JSONL, (4) asserts entry structure matches expected schema</test>
  <action>GREEN phase: Write helper appendExecutionLogEntry(cwd, entry) that appends JSONL to .planning/execution-log.jsonl; test writes 2 entries (pass and fail status); read file, split by newlines, JSON.parse each; assert phase/plan/task/status/commit fields present</action>
  <refactor>Consider extracting appendExecutionLogEntry to test-utils for reuse; ensure cleanup removes execution-log.jsonl</refactor>
  <verify>Test passes; execution-log.jsonl contains valid JSONL with expected fields</verify>
  <done>Integration test verifies execution log JSONL format compatible with draht-post-task.js hook</done>
</task>

---
Created: 2026-03-11 12:22:41
