---
phase: 21
plan: 2
depends_on: [1]
must_haves:
  - "Full lifecycle test from planning scaffold to verification"
  - "Real git commits with scoped commit messages"
  - "Execution log JSONL assertions"
---

# Phase 21, Plan 2: Full Lifecycle Integration Test

## Goal
Verify the real GSD lifecycle works end to end in an isolated repo with real planning artifacts and git operations.

## Context
- Depends on Plan 1 for temp git repo setup.
- Covers the main acceptance path for R21-INT.1.
- Work stays inside the GSD lifecycle boundary in `packages/coding-agent` tests.

## Tasks

<task type="auto">
  <n>Build a realistic Phase 21 test workspace fixture</n>
  <context>GSD lifecycle integration</context>
  <domain>Phase, Plan, Project, Summary</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>
    RED phase: Add a failing setup-oriented lifecycle spec that boots a temp git repo and requires minimal `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` inputs needed by GSD functions.
  </test>
  <action>
    GREEN phase: Compose the shared temp repo helper with the smallest realistic planning scaffold for Phase 21 so lifecycle functions run against a real workspace without mocks.
  </action>
  <refactor>
    REFACTOR phase: Extract local fixture builders only if repeated setup obscures the lifecycle assertions.
  </refactor>
  <verify>Run the targeted test file and confirm the fixture is writable and resolves to the Phase 21 directory correctly.</verify>
  <done>Passing setup assertions prove the test repo is a valid planning workspace for lifecycle tests.</done>
</task>

<task type="auto">
  <n>Test create-domain-model through commit-task in one real repo</n>
  <context>GSD lifecycle integration</context>
  <domain>Phase, Plan, Task, CommitResult</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>
    RED phase: Add a failing integration test that:
    - creates planning scaffold input
    - calls `createDomainModel()`
    - calls `createPlan(21, 2, ...)`
    - writes a real file change
    - calls `commitTask(21, 2, ...)`
    - asserts a commit hash exists
    - asserts `git log --format=%s -1` matches `feat(21-02): ...`
  </test>
  <action>
    GREEN phase: Use the real exported GSD functions with the temp repo. Assert observable artifacts instead of implementation details.
  </action>
  <refactor>
    REFACTOR phase: Extract small path helpers if repeated phase-plan filenames reduce readability.
  </refactor>
  <verify>Run the lifecycle test file and confirm the repo contains the created artifacts and a real scoped commit.</verify>
  <done>Passing assertions prove the workflow from domain-model generation to task commit works in a real repo.</done>
</task>

<task type="auto">
  <n>Test summary writing, phase verification, and execution log shape</n>
  <context>GSD lifecycle integration</context>
  <domain>Phase, Plan, Summary, ExecutionLog</domain>
  <files>packages/coding-agent/test/gsd-lifecycle.test.ts</files>
  <test>
    RED phase: Extend the integration coverage so it fails until:
    - `writeSummary()` creates a summary file
    - `verifyPhase(21)` reports completion when plans and summaries align
    - `21-VERIFICATION.md` is created
    - `.planning/execution-log.jsonl` parses as valid JSONL with `timestamp`, `phase`, `plan`, `task`, `status`, and `commit`
  </test>
  <action>
    GREEN phase: Add one end-to-end assertion path that writes summaries, verifies the phase, and validates execution-log lines as strict JSON objects compatible with hook expectations.
  </action>
  <refactor>
    REFACTOR phase: Extract a tiny JSONL parser helper only if repeated parsing logic appears.
  </refactor>
  <verify>Run the lifecycle test file and confirm all lifecycle artifacts are created and execution-log lines parse cleanly.</verify>
  <done>Passing assertions prove the repo can reach a verified phase state with valid summary and execution-log artifacts.</done>
</task>

---
Created: 2026-03-12 19:30:28
