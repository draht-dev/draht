---
phase: 21
plan: 3
depends_on: [1]
must_haves:
  - "Structured domain extraction assertions"
  - "Known extracted terms: Order, Customer, OrderItem"
  - "Defined behavior for empty or missing inputs"
---

# Phase 21, Plan 3: map-codebase Integration Test

## Goal
Verify `mapCodebase` produces deterministic domain extraction results for a known fixture codebase.

## Context
- Depends on Plan 1 for the stable domain fixture.
- Covers R21-INT.2.
- Keep domain extraction logic and its test surface inside the GSD bounded context.

## Tasks

<task type="auto">
  <n>Define failing integration coverage for structured domain extraction</n>
  <context>GSD domain extraction</context>
  <domain>Order, Customer, OrderItem</domain>
  <files>packages/coding-agent/test/gsd-map-codebase.test.ts</files>
  <test>
    RED phase: Add a dedicated integration suite that points `mapCodebase` at the domain fixture and fails until the result exposes structured extraction data.
    - result has `entities` array
    - result has `valueObjects` array
    - extracted terms include `Order`
    - extracted terms include `Customer`
    - extracted terms include `OrderItem`
    - empty input returns defined empty arrays
    - missing input fails in a defined, asserted way
  </test>
  <action>
    GREEN phase: Do not change implementation yet; let the new assertions define the contract first.
  </action>
  <refactor>
    REFACTOR phase: Extract shared fixture-path setup inside the test file if repeated.
  </refactor>
  <verify>Run the map-codebase test file and confirm failures are limited to missing structured extraction behavior.</verify>
  <done>Failing tests clearly define the required extraction contract and edge behavior.</done>
</task>

<task type="auto">
  <n>Add typed extraction results to GSD domain mapping</n>
  <context>GSD domain extraction</context>
  <domain>Order, Customer, OrderItem</domain>
  <files>packages/coding-agent/src/gsd/domain.ts</files>
  <test>
    RED phase: Use the failing integration suite as the driver for the structured extraction contract.
  </test>
  <action>
    GREEN phase: Update domain mapping so `mapCodebase` derives deterministic extracted terms into typed arrays suitable for assertions while preserving its existing documentation side effects.
  </action>
  <refactor>
    REFACTOR phase: Isolate scanning, term parsing, and empty-input fallback into small internal helpers to keep extraction deterministic and readable.
  </refactor>
  <verify>Run the map-codebase tests and confirm structured arrays contain the expected terms from the fixture.</verify>
  <done>Passing assertions prove `mapCodebase` returns structured extraction data with the expected Phase 21 fixture terms.</done>
</task>

<task type="auto">
  <n>Export the structured mapping contract through the public GSD entrypoint</n>
  <context>GSD public API</context>
  <domain>Phase, Plan</domain>
  <files>packages/coding-agent/src/gsd/index.ts, packages/coding-agent/test/gsd-map-codebase.test.ts</files>
  <test>
    RED phase: Extend the integration suite to import the mapping function and any required result type through the public GSD entrypoint.
  </test>
  <action>
    GREEN phase: Re-export the updated mapping API from `src/gsd/index.ts` so downstream callers can use the structured extraction contract through the package surface.
  </action>
  <refactor>
    REFACTOR phase: Keep the public surface minimal and avoid duplicate type definitions.
  </refactor>
  <verify>Run the targeted test file and confirm public imports succeed with the new structured result contract.</verify>
  <done>Passing assertions prove the structured map-codebase contract is available through the public GSD entrypoint.</done>
</task>

---
Created: 2026-03-12 19:30:28
