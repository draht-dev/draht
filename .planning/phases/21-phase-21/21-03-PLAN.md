---
phase: 21
plan: 3
depends_on: [1]
must_haves:
  - "map-codebase test produces valid domain extraction"
  - "Structure validation (entities, valueObjects arrays)"
  - "Known terms extracted (Order, Customer, OrderItem)"
---

# Phase 21, Plan 3: map-codebase Integration Test

## Goal
Verify map-codebase correctly extracts domain terms from a fixture codebase.

## Context
- R21-INT.2: map-codebase test produces valid domain extraction
- Uses domain fixture from Plan 1 (Order, Customer, OrderItem)
- Both structure and content validation

## Tasks

<task type="auto">
  <n>Add DomainExtraction type and extractDomain function</n>
  <context>GSD</context>
  <domain>DomainExtraction, Entity, ValueObject</domain>
  <files>packages/coding-agent/src/gsd/domain.ts, packages/coding-agent/src/gsd/index.ts</files>
  <test>RED phase: Write gsd-map-codebase.test.ts importing extractDomain, calling it on domain-fixture path, asserting result has entities array, valueObjects array, and contains Order/Customer/OrderItem. Tests fail because extractDomain doesn't exist.</test>
  <action>GREEN phase: Add DomainExtraction interface with entities: string[] and valueObjects: string[]. Add extractDomain(cwd: string): DomainExtraction function that parses TypeScript files for exported interfaces/classes, categorizes them based on naming conventions (entities are PascalCase domain objects, value objects have ValueObject/VO suffix or are in value-objects dir). Export from index.ts.</action>
  <refactor>REFACTOR phase: Extract regex patterns to constants. Consider sharing grep logic with existing mapCodebase domain hints extraction.</refactor>
  <verify>npm run check passes. extractDomain returns typed DomainExtraction object.</verify>
  <done>DomainExtraction type exported from @draht/coding-agent. extractDomain function exists and returns structured data.</done>
</task>

<task type="auto">
  <n>Integration test validates domain extraction from fixture</n>
  <context>GSD</context>
  <domain>Order, Customer, OrderItem</domain>
  <files>packages/coding-agent/test/gsd-map-codebase.test.ts</files>
  <test>RED phase: Complete test file with describe block for extractDomain integration. Test cases: (1) result.entities is array, (2) result.valueObjects is array, (3) entities includes "Order", (4) entities includes "Customer", (5) entities or valueObjects includes "OrderItem".</test>
  <action>GREEN phase: Ensure extractDomain implementation correctly identifies Order.ts, Customer.ts, OrderItem.ts from test/fixtures/domain-fixture/. Adjust regex patterns if needed to match fixture file conventions (export interface Order, export class Customer, etc).</action>
  <refactor>REFACTOR phase: Add edge case tests - empty directory returns empty arrays, non-existent directory throws.</refactor>
  <verify>npm run check passes. npx vitest --run test/gsd-map-codebase.test.ts passes with all assertions green.</verify>
  <done>R21-INT.2 satisfied: gsd-map-codebase.test.ts validates extractDomain produces valid domain extraction with entities/valueObjects arrays containing Order, Customer, OrderItem.</done>
</task>

---
Created: 2026-03-11 12:22:45
