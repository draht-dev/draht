---
phase: 21
plan: 4
depends_on: [1]
must_haves:
  - "Quality gate pass test (passing tests → exit 0)"
  - "Quality gate fail test (failing tests → exit 1)"
  - "TypeScript error detection test"
---

# Phase 21, Plan 4: Quality Gate Tests

## Goal
Verify the quality gate hook correctly reports pass/fail outcomes.

## Context
- R21-INT.3: Quality gate pass/fail test covers both outcomes
- Uses createTempGitRepo from Plan 1
- Tests both test execution failures and TypeScript errors

## Tasks

<task type="auto">
  <n>Quality gate pass test with passing test suite</n>
  <context>GSD</context>
  <domain>QualityGate, ToolchainInfo, HookConfig</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts</files>
  <test>RED: Create test file importing createTempGitRepo from test-utils/git-repo.ts. Write test "quality gate returns exit 0 when all tests pass" that: (1) creates temp repo with createTempGitRepo including package.json with passing test script, (2) adds a simple passing test file, (3) executes hooks/gsd/draht-quality-gate.js via execSync in temp repo cwd, (4) asserts exit code is 0 and stdout contains "Quality gate passed"</test>
  <action>GREEN: Since draht-quality-gate.js already exists and returns exit 0 on success, the test should pass once the temp repo setup is correct. Key setup: package.json needs "test": "echo passing" or minimal vitest config, ensure npm lockfile exists for toolchain detection</action>
  <refactor>REFACTOR: Extract test repo setup to helper function createPassingTestRepo() that returns repo path with all necessary files pre-configured</refactor>
  <verify>npm run check passes, test runs in isolation with npx tsx vitest --run test/gsd-quality-gate.test.ts</verify>
  <done>Test creates temp git repo, runs quality gate hook, asserts exit 0 and success message in stdout</done>
</task>

<task type="auto">
  <n>Quality gate fail test with failing test suite</n>
  <context>GSD</context>
  <domain>QualityGate, ToolchainInfo, HookConfig</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts</files>
  <test>RED: Add test "quality gate returns exit 1 when tests fail" that: (1) creates temp repo with createTempGitRepo, (2) configures package.json with "test": "exit 1" script to simulate failure, (3) adds .planning/config.json with qualityGateStrict: true, (4) executes draht-quality-gate.js, (5) catches the error (execSync throws on non-zero exit), (6) asserts error.status is 1 and output contains "FAILED"</test>
  <action>GREEN: The hook already exits 1 on errors when strict mode is enabled. Ensure the test properly wraps execSync in try-catch and checks error.status property for exit code</action>
  <refactor>REFACTOR: Extract test repo setup to helper function createFailingTestRepo() that returns repo path with failing test configuration</refactor>
  <verify>npm run check passes, test confirms both pass and fail branches of quality gate are covered</verify>
  <done>Test creates temp git repo with failing tests, runs quality gate hook in strict mode, asserts exit 1 and failure message</done>
</task>

<task type="auto">
  <n>Quality gate TypeScript error detection test</n>
  <context>GSD</context>
  <domain>QualityGate, ToolchainInfo</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts</files>
  <test>RED: Add test "quality gate detects TypeScript errors in strict mode" that: (1) creates temp repo with tsconfig.json and a .ts file with intentional type error, (2) configures qualityGateStrict: true, (3) runs quality gate, (4) asserts exit 1 and output mentions "TypeScript error(s)"</test>
  <action>GREEN: The hook already runs tsc --noEmit and reports TypeScript errors. Ensure temp repo has minimal tsconfig.json and a file like "const x: number = 'string';" to trigger error</action>
  <refactor>REFACTOR: Consolidate repo creation helpers, ensure cleanup in afterEach removes all temp repos. Add shared constants for hook path resolution using import.meta.url</refactor>
  <verify>npm run check passes, all three quality gate test scenarios (pass, test fail, TS error) are covered with real temp repos</verify>
  <done>Comprehensive quality gate tests cover pass/fail outcomes for both test execution and TypeScript checking, R21-INT.3 satisfied</done>
</task>

---
Created: 2026-03-11 12:22:50
