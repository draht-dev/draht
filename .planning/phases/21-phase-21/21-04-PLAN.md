---
phase: 21
plan: 4
depends_on: [1]
must_haves:
  - "Quality gate success path test"
  - "Quality gate failing-test path test"
  - "Quality gate TypeScript-error path test"
---

# Phase 21, Plan 4: Quality Gate Tests

## Goal
Verify the real quality gate hook reports success and failure correctly across the main strict-mode outcomes.

## Context
- Depends on Plan 1 for isolated temp repos.
- Covers R21-INT.3.
- Tests should execute the real hook script, not mocks.

## Tasks

<task type="auto">
  <n>Create a real-hook quality gate test harness</n>
  <context>GSD hook integration</context>
  <domain>QualityGate, HookConfig, ToolchainInfo</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts</files>
  <test>
    RED phase: Add a Vitest suite that creates isolated repos, writes minimal package/test inputs, invokes the real `draht-quality-gate.js` script, and captures exit status plus stdout/stderr for assertions.
  </test>
  <action>
    GREEN phase: Build small typed helpers inside the test file so each scenario executes the real hook against a temp repo without mocking process behavior.
  </action>
  <refactor>
    REFACTOR phase: Normalize repeated repo bootstrap and hook-execution logic into local helpers only as needed.
  </refactor>
  <verify>Run the targeted test file and confirm the harness can execute the hook deterministically.</verify>
  <done>Passing setup assertions prove the suite can run the real quality gate hook and inspect its process outcome.</done>
</task>

<task type="auto">
  <n>Cover passing and failing test-suite outcomes</n>
  <context>GSD hook integration</context>
  <domain>QualityGate, HookConfig</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts, hooks/gsd/draht-quality-gate.js</files>
  <test>
    RED phase: Add two failing scenarios:
    - repo with passing tests exits 0 and surfaces a success signal
    - strict-mode repo with failing tests exits non-zero and surfaces failure output
  </test>
  <action>
    GREEN phase: Adjust only the test fixtures unless the integration test proves the hook misreports the observable outcome; if so, patch the hook minimally.
  </action>
  <refactor>
    REFACTOR phase: Consolidate shared fixture pieces while keeping success and failure assertions explicit.
  </refactor>
  <verify>Run the quality-gate test file and confirm both the success branch and failing-test branch are covered.</verify>
  <done>Passing assertions prove the gate succeeds for green repos and fails loudly for strict-mode failing tests.</done>
</task>

<task type="auto">
  <n>Cover strict-mode TypeScript error detection</n>
  <context>GSD hook integration</context>
  <domain>QualityGate, ToolchainInfo</domain>
  <files>packages/coding-agent/test/gsd-quality-gate.test.ts, hooks/gsd/draht-quality-gate.js</files>
  <test>
    RED phase: Add a failing scenario where tests pass but TypeScript compilation fails, and assert the gate exits non-zero with surfaced TypeScript failure output.
  </test>
  <action>
    GREEN phase: Keep the hook unchanged unless the new integration test proves TypeScript failures are swallowed, misclassified, or reported incorrectly.
  </action>
  <refactor>
    REFACTOR phase: Keep TypeScript-error fixtures separate from failing-test fixtures so each scenario asserts one quality signal.
  </refactor>
  <verify>Run the quality-gate test file and confirm TypeScript errors fail the gate independently of the test command result.</verify>
  <done>Passing assertions prove strict-mode TypeScript errors produce a non-zero quality-gate result with visible failure output.</done>
</task>

---
Created: 2026-03-12 19:30:28
