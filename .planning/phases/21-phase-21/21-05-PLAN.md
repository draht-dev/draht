---
phase: 21
plan: 5
depends_on: []
must_haves:
  - "Extension loads without error"
  - "All expected commands registered"
  - "Commands are callable (smoke test)"
---

# Phase 21, Plan 5: Extension Loading Test

## Goal
Verify gsd-commands.ts extension loads correctly and registers all commands.

## Context
- R21-INT.4: gsd-commands extension loading test confirms registration
- Runtime loading verification (not just source structure checking)
- Smoke test invocation to confirm commands are callable

## Tasks

<task type="auto">
  <n>Create runtime extension loading test with failure cases</n>
  <context>GSD</context>
  <domain>Extension, ExtensionRunner, RegisteredCommand</domain>
  <files>packages/coding-agent/test/gsd-extension-loading.test.ts</files>
  <test>RED phase: Create test file with describe block for gsd-commands extension runtime loading. Write failing tests:
1. "loads gsd-commands.ts extension without error" - use discoverAndLoadExtensions with explicit path to templates/project/.draht/extensions/gsd-commands.ts, assert result.errors.length === 0
2. "registers all expected GSD commands" - assert extension.commands.has() for: create-plan, commit-task, create-domain-model, map-codebase, plus workflow commands: discuss, plan, execute, verify, review, fix, quick, resume, status, new-project, init-project
3. "ExtensionRunner.getCommand() returns registered commands" - create ExtensionRunner from loaded extensions, call getCommand() for each planning command, assert each returns defined command with correct name</test>
  <action>GREEN phase: Tests should pass immediately since gsd-commands.ts already exists and exports correct factory. If any fail, diagnose: check path resolution, verify extension exports default function, check command registration names match exactly</action>
  <refactor>REFACTOR phase: Extract shared setup (tempDir creation, SessionManager.inMemory(), AuthStorage, ModelRegistry) into beforeEach. Extract command name arrays into constants: PLANNING_COMMANDS = ["create-plan", "commit-task", "create-domain-model", "map-codebase"], WORKFLOW_COMMANDS = ["discuss", "plan", "execute", ...]. Use it.each for repetitive command existence checks</refactor>
  <verify>npm run check from packages/coding-agent. All tests pass, no type errors</verify>
  <done>Test file exists at packages/coding-agent/test/gsd-extension-loading.test.ts with 3 passing tests verifying extension loads and all commands register</done>
</task>

<task type="auto">
  <n>Add smoke test for command invocation</n>
  <context>GSD</context>
  <domain>RegisteredCommand, ExtensionCommandContext</domain>
  <files>packages/coding-agent/test/gsd-extension-loading.test.ts</files>
  <test>RED phase: Add new describe block "command invocation smoke test". Write failing test:
1. "create-plan command handler can be invoked" - get command via runner.getCommand("create-plan"), create mock ExtensionCommandContext with: ui.notify mock, cwd pointing to temp dir with .planning/ folder, isIdle returning true. Call command.handler("", mockCtx) and assert ui.notify was called with "Usage:" warning (empty args case)</test>
  <action>GREEN phase: Create minimal mock ExtensionCommandContext satisfying the interface: ui object with notify spy, cwd set to tempDir, isIdle returning true, other methods as no-ops or returning safe defaults. Invoke handler, verify notify called. If handler throws, check what ctx properties it accesses and add those to mock</action>
  <refactor>REFACTOR phase: Extract createMockCommandContext(tempDir) helper function returning typed ExtensionCommandContext mock. Add JSDoc explaining this is a smoke test verifying command is callable, not testing full behavior</refactor>
  <verify>npm run check from packages/coding-agent. All tests pass including smoke test</verify>
  <done>Smoke test passes, verifying create-plan command handler is callable and responds to empty args with usage warning</done>
</task>

---
Created: 2026-03-11 12:22:55
