---
phase: 21
plan: 5
depends_on: []
must_haves:
  - "Runtime extension loading succeeds"
  - "Expected commands are registered"
  - "At least one command handler is callable"
---

# Phase 21, Plan 5: Extension Loading Test

## Goal
Verify the shipped `gsd-commands` extension loads at runtime, registers the expected commands, and exposes callable handlers.

## Context
- Independent of Plans 1-4.
- Covers R21-INT.4.
- Replace structure-only checks with runtime loading assertions.

## Tasks

<task type="auto">
  <n>Replace structure-only checks with runtime extension loading coverage</n>
  <context>GSD extension runtime</context>
  <domain>Extension, RegisteredCommand</domain>
  <files>packages/coding-agent/test/gsd-extension-loading.test.ts</files>
  <test>
    RED phase: Add a runtime loading spec that loads `templates/project/.draht/extensions/gsd-commands.ts` through the real extension loader and fails until:
    - loader reports zero errors
    - expected planning commands are registered
    - expected workflow commands are registered
  </test>
  <action>
    GREEN phase: Use the existing extension loading APIs so the test verifies real module evaluation rather than source-string inspection.
  </action>
  <refactor>
    REFACTOR phase: Extract expected command-name arrays into local constants and remove brittle structure-only assertions.
  </refactor>
  <verify>Run the targeted extension-loading test file and confirm runtime loading succeeds with the expected command registrations.</verify>
  <done>Passing assertions prove the real `gsd-commands` extension loads without runtime errors and registers the expected commands.</done>
</task>

<task type="auto">
  <n>Assert runtime registry lookup by command name</n>
  <context>GSD extension runtime</context>
  <domain>Extension, RegisteredCommand</domain>
  <files>packages/coding-agent/test/gsd-extension-loading.test.ts</files>
  <test>
    RED phase: Extend the runtime test so it fails unless the runtime registry returns concrete command objects for names like `create-plan`, `commit-task`, and one workflow command.
  </test>
  <action>
    GREEN phase: Wrap the loaded extension in the existing command runner/runtime and assert lookup behavior through the public registry methods.
  </action>
  <refactor>
    REFACTOR phase: Extract small typed runner/context builders inside the test file if setup repeats.
  </refactor>
  <verify>Run the extension-loading test file and confirm registry lookups succeed for expected names and fail cleanly for unknown names.</verify>
  <done>Passing assertions prove runtime command lookup works for the loaded GSD extension.</done>
</task>

<task type="auto">
  <n>Smoke-test one real command handler</n>
  <context>GSD extension runtime</context>
  <domain>Extension, RegisteredCommand</domain>
  <files>packages/coding-agent/test/gsd-extension-loading.test.ts, templates/project/.draht/extensions/gsd-commands.ts, packages/coding-agent/src/core/extensions/loader.ts, packages/coding-agent/src/core/extensions/runner.ts</files>
  <test>
    RED phase: Add a failing smoke test that fetches `create-plan` from the runtime registry, invokes its handler with empty args and a typed mock command context, and asserts `ui.notify` receives the expected usage warning.
  </test>
  <action>
    GREEN phase: Implement only the smallest test harness needed for invocation. Patch the extension or loader only if the runtime test exposes a real loading or invocation gap.
  </action>
  <refactor>
    REFACTOR phase: Extract reusable typed mock UI/context builders inside the test file and keep production changes minimal.
  </refactor>
  <verify>Run the extension-loading test file and confirm at least one real command handler is callable and emits the expected warning on empty args.</verify>
  <done>Passing assertions prove a runtime-loaded GSD command handler can be invoked safely through the registered command object.</done>
</task>

---
Created: 2026-03-12 19:30:28
