---
phase: 22
plan: 1
depends_on: []
must_haves:
  - "Fallback works transparently when provider fails"
  - "Mid-stream failures switch providers without state leakage"
  - "Non-retryable errors fail fast without fallback"
---

# Phase 22, Plan 1: Fallback Chain Integration Tests

## Goal
Verify that the model router's fallback chain works reliably under all failure scenarios including mid-stream errors.

## Context
- Provider failure simulation uses error injection callbacks (not mocking @draht/ai internals)
- Tests must cover both stream() and streamSimple() methods
- Error classification determines whether fallback is attempted

## Tasks

<task type="auto">
  <n>Create integration test infrastructure with controllable mock API providers</n>
  <context>Model Router (packages/router/) integration with @draht/ai API provider registry</context>
  <domain>ApiProvider registration, Model resolution, Provider stream functions</domain>
  <files>packages/router/test/fallback.test.ts</files>
  <test>
    RED phase: Create test file with mock provider infrastructure that fails.
    - Test imports: ModelRouter, registerApiProvider, clearApiProviders from @draht/ai
    - Create MockProvider class that can be configured to:
      - Fail immediately before yielding any events
      - Yield N events then fail
      - Succeed after yielding events
    - Register two mock APIs: test-primary and test-fallback with mock models
    - Create router config with primary and fallback for test-role
    - Write first test: primary fails immediately, fallback succeeds
    - Expected: Test FAILS because mock infrastructure is not yet implemented
  </test>
  <action>
    GREEN phase: Implement mock provider infrastructure.
    - Create async generator function createMockStream that accepts config:
      - eventsBeforeFailure: number (0 = fail immediately)
      - errorMessage: string
      - shouldFail: boolean
    - Implement registerTestProvider helper that registers a provider with configurable behavior
    - Use @draht/ai registerApiProvider to register test-primary and test-fallback APIs
    - Implement cleanup using clearApiProviders in afterEach
    - Make first test pass by yielding text events from fallback provider
  </action>
  <refactor>
    REFACTOR phase: Extract test utilities.
    - Move MockStreamConfig interface and createMockStream into dedicated helper
    - Extract provider registration into reusable registerMockProvider function
    - Ensure test names clearly describe the fallback scenario being tested
    - Add JSDoc comments explaining mock provider behavior
  </refactor>
  <verify>Run bun test packages/router/test/fallback.test.ts - first test passes, mock providers register and stream correctly</verify>
  <done>Test passes: router.stream() falls back from failing primary to succeeding fallback provider, emits text events from fallback</done>
</task>

<task type="auto">
  <n>Test streaming fallback scenarios for mid-stream failures and both stream methods</n>
  <context>Model Router stream() and streamSimple() methods with partial response handling</context>
  <domain>AssistantMessageEvent streams, partial content accumulation, mid-stream error recovery</domain>
  <files>packages/router/test/fallback.test.ts, packages/router/src/router.ts</files>
  <test>
    RED phase: Write tests for mid-stream failures and streamSimple.
    - Test: Primary emits 3 text events then fails, fallback continues
      - Assert: All events from fallback are received, no primary events leak through
      - Assert: Final accumulated text is from fallback only
    - Test: Both stream() and streamSimple() methods handle fallback identically
      - Create identical failure scenario for both methods
      - Assert: Both methods fall back to same provider and emit same events
    - Test: Fallback provider also fails mid-stream
      - Primary emits 2 events then fails, fallback emits 1 event then fails
      - Assert: Throws error from fallback (last in chain)
    - Expected: Tests FAIL revealing potential issues with mid-stream state handling
  </test>
  <action>
    GREEN phase: Fix any issues found during RED phase.
    - If router.stream() does not properly reset state between fallback attempts, add state reset logic
    - If partial events from failed primary leak into fallback stream, fix generator cleanup
    - Ensure streamSimple() follows same fallback path as stream()
    - Add error context preservation so thrown errors include which provider failed
    - Minimal changes to make tests pass
  </action>
  <refactor>
    REFACTOR phase: Improve error handling and logging.
    - Extract stream attempt logic into private helper method if duplicated between stream() and streamSimple()
    - Add error wrapping that preserves stack trace and adds fallback context
    - Ensure error messages include role name and attempted provider sequence
    - Document mid-stream failure behavior in JSDoc comments on stream methods
  </refactor>
  <verify>Run bun test packages/router/test/fallback.test.ts - all mid-stream tests pass, both stream methods handle fallback correctly</verify>
  <done>Tests pass: router handles mid-stream failures without state leakage, streamSimple() has identical fallback behavior to stream(), errors preserve context</done>
</task>

<task type="auto">
  <n>Test error classification and fallback boundaries</n>
  <context>Model Router retryable error detection and non-retryable error handling</context>
  <domain>Error classification (isRetryableError), fallback chain exhaustion, fast-fail semantics</domain>
  <files>packages/router/test/fallback.test.ts, packages/router/src/router.ts</files>
  <test>
    RED phase: Write tests for error boundary conditions.
    - Test: Non-retryable error (auth failure) does not trigger fallback
      - Primary fails with "invalid API key" error
      - Assert: Error thrown immediately without attempting fallback provider
      - Assert: Error message is original error, not "No available model"
    - Test: All providers in chain fail with retryable errors
      - Primary fails with 429, fallback fails with 503
      - Assert: Throws last error (503 from fallback)
      - Assert: Error message includes context about failed chain
    - Test: Mixed retryable and non-retryable errors
      - Primary fails with 429 (retryable), fallback fails with 401 (non-retryable)
      - Assert: Throws 401 error immediately when fallback fails
    - Test: Empty fallback chain (role with no fallbacks)
      - Primary fails with retryable error, no fallbacks configured
      - Assert: Throws original error with helpful message
    - Expected: Tests FAIL if isRetryableError classification is incomplete or fallback logic has edge case bugs
  </test>
  <action>
    GREEN phase: Fix error classification and fallback logic bugs.
    - Expand isRetryableError to cover any missing error patterns found during testing
    - Ensure non-retryable errors short-circuit fallback loop immediately
    - Fix error message when all providers fail to include attempted provider list
    - Handle empty fallback array gracefully with clear error message
    - Add error cause chaining so original errors are preserved
  </action>
  <refactor>
    REFACTOR phase: Improve error classification and messaging.
    - Extract error classification tests into isRetryableError unit tests if not already comprehensive
    - Create custom error class RouterError with fields: role, attemptedProviders, originalError
    - Throw RouterError instead of raw errors for better debugging context
    - Document error classification rules in JSDoc on isRetryableError function
    - Add example error messages to README.md showing what users see on failures
  </refactor>
  <verify>Run bun test packages/router/test/fallback.test.ts and bun test packages/router/test/router.test.ts - all error handling tests pass, error messages are clear and actionable</verify>
  <done>Tests pass: non-retryable errors fail fast without fallback, all-fail scenarios throw last error with full context, empty fallback chains handled gracefully, error messages include role and provider chain</done>
</task>
