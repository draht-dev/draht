---
phase: 22
plan: 2
depends_on: []
must_haves:
  - "Cost calculations match expected values within 1% tolerance"
  - "Unknown models use default rates correctly"
  - "Reasoning tokens calculated at input rate"
---

# Phase 22, Plan 2: Cost Tracking Accuracy Tests

## Goal
Verify that cost tracking is accurate across all model types, edge cases, and reasoning token scenarios.

## Context
- Cost calculations must match COST_PER_MILLION lookup table exactly (within 1% floating-point tolerance)
- Missing models use default rates { input: 3, output: 15 }
- Test strategy uses fixture-based tests with known token counts → expected costs

## Tasks

<task type="auto">
  <n>Test cost calculation accuracy for known models</n>
  <context>Cost Tracking - Model Router</context>
  <domain>CostEntry, estimateCost value object</domain>
  <files>packages/router/test/cost-tracking.test.ts, packages/router/src/cost.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - Test anthropic/claude-opus-4-6 with 100000 input, 50000 output tokens → expect $2.25 USD (0.1M * $15 + 0.05M * $75)
    - Test google/gemini-2.5-flash with 1000000 input, 500000 output tokens → expect $0.45 USD (1M * $0.15 + 0.5M * $0.6)
    - Test deepseek/deepseek-v3 with 500000 input, 200000 output tokens → expect $0.355 USD (0.5M * $0.27 + 0.2M * $1.1)
    - Test openai/gpt-5.2 with 200000 input, 100000 output tokens → expect $2.50 USD (0.2M * $5 + 0.1M * $15)
    - All tests must verify result is within 1% tolerance of expected value
    - Tests MUST fail before implementation exists
  </test>
  <action>
    GREEN phase: Create test file with fixtures.
    - Create packages/router/test/cost-tracking.test.ts using Bun test framework
    - Import estimateCost from packages/router/src/cost.ts
    - Write test cases with exact token counts and expected costs from COST_PER_MILLION table
    - Use expect(actual).toBeCloseTo(expected, 5) for floating-point comparison with 1% tolerance
    - Verify existing estimateCost implementation passes all tests
  </action>
  <refactor>
    REFACTOR phase: Extract test fixtures and tolerance helper.
    - Extract cost calculation test cases into structured fixtures array
    - Create helper function assertCostWithinTolerance(actual, expected) for 1% tolerance check
    - Ensure test names clearly express business rules: "calculates cost for X at rate Y"
    - Keep test data close to pricing table for easy verification
  </refactor>
  <verify>Run bun test packages/router/test/cost-tracking.test.ts - all tests pass</verify>
  <done>All known model cost calculations match expected values within 1% tolerance based on COST_PER_MILLION pricing table</done>
</task>

<task type="auto">
  <n>Test default rates for unknown models</n>
  <context>Cost Tracking - Model Router</context>
  <domain>estimateCost value object, default pricing policy</domain>
  <files>packages/router/test/cost-tracking.test.ts, packages/router/src/cost.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - Test unknown-provider/unknown-model with 100000 input, 50000 output → expect $1.05 USD (0.1M * $3 + 0.05M * $15)
    - Test anthropic/claude-opus-5 (unknown model, known provider) with 500000 input, 250000 output → expect $5.25 USD (0.5M * $3 + 0.25M * $15)
    - Test completely-new-provider/new-model with 1000000 input, 0 output → expect $3.00 USD (1M * $3 + 0 * $15)
    - Verify fallback rates are { input: 3, output: 15 } as defined in cost.ts
    - Tests MUST fail before implementation
  </test>
  <action>
    GREEN phase: Add test cases to cost-tracking.test.ts.
    - Add describe block "unknown models use default rates"
    - Write three test cases verifying default rate application
    - Verify existing estimateCost implementation already handles this via nullish coalescing
    - Confirm tests pass with current implementation
  </action>
  <refactor>
    REFACTOR phase: Document default pricing policy.
    - Add JSDoc comment to estimateCost explaining default rate policy
    - Extract default rates constant DEFAULT_RATES = { input: 3, output: 15 }
    - Reference DEFAULT_RATES in both implementation and tests
    - Ensure ubiquitous language: "default rates" not "fallback" or "unknown model rates"
  </refactor>
  <verify>Run bun test packages/router/test/cost-tracking.test.ts - unknown model tests pass</verify>
  <done>Unknown model IDs correctly use default rates { input: 3, output: 15 } with results within 1% tolerance</done>
</task>

<task type="auto">
  <n>Add reasoning token support to cost calculations</n>
  <context>Cost Tracking - Model Router</context>
  <domain>CostEntry, estimateCost value object, reasoning token pricing</domain>
  <files>packages/router/src/cost.ts, packages/router/src/types.ts, packages/router/test/cost-tracking.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - Test anthropic/claude-sonnet-4-6 with 100000 input, 50000 output, 25000 reasoning → expect $1.425 USD (reasoning billed as input tokens: (0.1M + 0.025M) * $3 + 0.05M * $15)
    - Test openai/gpt-5.2 with 200000 input, 100000 output, 50000 reasoning → expect $2.75 USD ((0.2M + 0.05M) * $5 + 0.1M * $15)
    - Test unknown model with reasoning tokens uses default input rate for reasoning
    - Test backward compatibility: estimateCost without reasoning param still works
    - Tests MUST fail before reasoningTokens parameter added
  </test>
  <action>
    GREEN phase: Extend estimateCost signature and CostEntry.
    - Add optional reasoningTokens parameter to estimateCost(provider, model, inputTokens, outputTokens, reasoningTokens = 0)
    - Modify cost calculation: reasoning tokens billed at input token rate
    - Add optional reasoningTokens?: number field to CostEntry interface in types.ts
    - Update ModelRouter.trackCost to accept optional reasoningTokens parameter
    - Keep backward compatibility: reasoningTokens defaults to 0
  </action>
  <refactor>
    REFACTOR phase: Extract token cost calculation logic.
    - Create calculateTokenCost(tokens: number, ratePerMillion: number) helper function
    - Use helper for input, output, and reasoning token calculations
    - Ensure reasoning token business rule is explicit: "reasoning tokens billed at input rate"
    - Add JSDoc explaining reasoning token pricing model
  </refactor>
  <verify>Run bun test packages/router/test/cost-tracking.test.ts - reasoning token tests pass</verify>
  <done>Reasoning tokens are correctly calculated at input token rate, CostEntry tracks reasoning tokens, backward compatibility maintained</done>
</task>

<task type="auto">
  <n>Test edge cases and boundary conditions</n>
  <context>Cost Tracking - Model Router</context>
  <domain>estimateCost value object, boundary conditions</domain>
  <files>packages/router/test/cost-tracking.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - Test zero tokens: estimateCost with 0 input, 0 output → expect $0.00
    - Test zero output: anthropic/claude-opus-4-6 with 1000000 input, 0 output → expect $15.00 (1M * $15)
    - Test zero input: google/gemini-2.5-pro with 0 input, 1000000 output → expect $10.00 (1M * $10)
    - Test very large token counts: 100000000 input, 50000000 output for deepseek/deepseek-v3 → expect $82.00 (100M * $0.27 + 50M * $1.1)
    - Test single token: anthropic/claude-sonnet-4-6 with 1 input, 1 output → expect $0.000003015 (very small but non-zero)
    - All results within 1% tolerance
  </test>
  <action>
    GREEN phase: Add edge case tests.
    - Add describe block "edge cases and boundary conditions"
    - Write five test cases covering zero tokens, large counts, and single token scenarios
    - Verify existing estimateCost handles all cases correctly without modification
    - Confirm no division by zero, overflow, or precision loss issues
  </action>
  <refactor>
    REFACTOR phase: Group related test cases.
    - Organize test file with clear describe blocks: "known models", "unknown models", "reasoning tokens", "edge cases"
    - Extract common assertion logic into reusable matchers
    - Document expected behavior for edge cases in test names
    - Ensure test coverage report includes all boundary conditions
  </refactor>
  <verify>Run bun test packages/router/test/cost-tracking.test.ts --coverage - 100% branch coverage for estimateCost</verify>
  <done>Edge cases handle zero tokens, very large counts, and single tokens correctly without errors or precision loss</done>
</task>
