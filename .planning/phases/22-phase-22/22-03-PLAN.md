---
phase: 22
plan: 3
depends_on: []
must_haves:
  - "Invalid configs rejected with clear error messages"
  - "All built-in roles validated at load and save time"
  - "Provider and model existence validated against @draht/ai registry"
  - "Circular dependencies detected and rejected"
---

# Phase 22, Plan 3: Config Validation Tests

## Goal
Ensure router config validation is comprehensive and rejects all invalid configurations with clear, actionable error messages.

## Context
- Validation timing: Both loadConfig() and saveConfig() validate
- Error format: Throw with detailed multi-line message listing all validation issues
- Built-in roles required: architect, implement, boilerplate, quick, review, docs

## Tasks

<task type="auto">
  <n>Validate config structure and built-in roles</n>
  <context>Router Configuration - Config Validation Domain</context>
  <domain>RouterConfig value object, RoleConfig entity, ModelRef value object, ConfigValidationError</domain>
  <files>packages/router/test/config-validation.test.ts, packages/router/src/config.ts</files>
  <test>
    RED: Create config-validation.test.ts with failing tests:
    - Test empty provider string throws ValidationError listing "Empty provider string for role X"
    - Test empty model string throws ValidationError listing "Empty model string for role X"
    - Test missing architect role throws ValidationError listing "Missing required role: architect"
    - Test missing each built-in role (implement, boilerplate, quick, review, docs) throws specific error
    - Test all 6 built-in roles missing throws ValidationError with all 6 listed
    - Test valid config with all built-in roles passes validation
    - Test config with built-in roles plus custom roles passes validation
    - Run tests: all should fail with "validateConfig is not defined"
  </test>
  <action>
    GREEN: Implement validateConfig in config.ts:
    - Create validateConfig(config: RouterConfig): void function
    - Check all 6 built-in roles present: architect, implement, boilerplate, quick, review, docs
    - Check no empty provider or model strings in any RoleConfig
    - Collect all validation errors and throw single error with multi-line message
    - Export validateConfig from config.ts and index.ts
    - Tests should now pass
  </action>
  <refactor>
    REFACTOR: Extract validation logic into domain:
    - Create ConfigValidationError class extending Error with errors: string[] property
    - Extract BUILT_IN_ROLES constant to types.ts
    - Extract validateModelRef(ref: ModelRef, context: string) helper
    - Ensure error messages use consistent format
  </refactor>
  <verify>Run: cd packages/router && bun test test/config-validation.test.ts - all structure validation tests pass</verify>
  <done>validateConfig exists, rejects empty strings and missing built-in roles, throws ConfigValidationError with all issues listed</done>
</task>

<task type="auto">
  <n>Validate provider and model against @draht/ai registry</n>
  <context>Router Configuration - Provider/Model Registry Integration</context>
  <domain>ModelRef value object validation, Provider registry integration, ConfigValidationError</domain>
  <files>packages/router/test/config-validation.test.ts, packages/router/src/config.ts</files>
  <test>
    RED: Add tests to config-validation.test.ts:
    - Test invalid provider "nonexistent-provider" throws ValidationError listing "Invalid provider 'nonexistent-provider' for role X (not in @draht/ai registry)"
    - Test valid provider with invalid model ID throws ValidationError listing "Invalid model 'fake-model' for provider 'anthropic' in role X"
    - Test multiple invalid providers and models reports all issues in single error
    - Test valid providers and models from @draht/ai registry passes
    - Run tests: should fail because validateConfig does not check registry
  </test>
  <action>
    GREEN: Enhance validateConfig in config.ts:
    - Import getProviders, getModels from @draht/ai
    - After structure validation, validate each ModelRef against registry
    - For primary and each fallback: check provider in getProviders(), check model in getModels(provider)
    - Add validation errors to collected list before throwing
    - Tests should now pass
  </action>
  <refactor>
    REFACTOR: Extract registry validation:
    - Create validateModelRefAgainstRegistry(ref: ModelRef, context: string): string[] helper
    - Cache valid providers list to avoid multiple getProviders() calls
    - Ensure error messages clearly identify role, position (primary vs fallback), and registry mismatch
  </refactor>
  <verify>Run: cd packages/router && bun test test/config-validation.test.ts - all registry validation tests pass</verify>
  <done>validateConfig rejects invalid providers and model IDs by checking @draht/ai registry, reports all issues</done>
</task>

<task type="auto">
  <n>Detect duplicate models and circular fallback dependencies</n>
  <context>Router Configuration - Fallback Chain Validation</context>
  <domain>RoleConfig fallback chain invariants, CircularDependencyDetector, ConfigValidationError</domain>
  <files>packages/router/test/config-validation.test.ts, packages/router/src/config.ts</files>
  <test>
    RED: Add tests to config-validation.test.ts:
    - Test duplicate model in fallback chain (primary + fallback or fallback + fallback same provider/model) throws ValidationError listing "Duplicate model anthropic/claude-sonnet-4-6 in fallback chain for role X"
    - Test circular dependency A→B, B→A throws ValidationError listing "Circular fallback dependency detected: architect → implement → architect"
    - Test circular dependency A→B→C→A throws ValidationError listing full cycle
    - Test self-reference (role fallback to same role via different model) throws ValidationError
    - Test valid non-circular fallback chains pass
    - Run tests: should fail because validateConfig does not check duplicates or cycles
  </test>
  <action>
    GREEN: Enhance validateConfig in config.ts:
    - For each RoleConfig, check no duplicate ModelRef (compare provider+model) in primary + fallbacks
    - Build dependency graph from all role configs
    - Implement cycle detection using depth-first search with visited/recursion stack
    - Add duplicate and cycle errors to collected list
    - Tests should now pass
  </action>
  <refactor>
    REFACTOR: Extract graph validation:
    - Create detectCircularDependencies(config: RouterConfig): string[] helper
    - Create modelRefEquals(a: ModelRef, b: ModelRef): boolean helper
    - Create formatModelRef(ref: ModelRef): string helper for consistent error display
    - Move graph traversal logic to separate function for clarity
  </refactor>
  <verify>Run: cd packages/router && bun test test/config-validation.test.ts - all duplicate and circular dependency tests pass</verify>
  <done>validateConfig detects and rejects duplicate models in fallback chains and circular role dependencies</done>
</task>

<task type="auto">
  <n>Integrate validation into loadConfig and saveConfig</n>
  <context>Router Configuration - Load/Save Lifecycle Validation</context>
  <domain>Config lifecycle, validation timing, error propagation</domain>
  <files>packages/router/test/config-validation.test.ts, packages/router/src/config.ts, packages/router/test/config.test.ts</files>
  <test>
    RED: Add integration tests to config-validation.test.ts:
    - Test loadConfig with invalid project config file throws ValidationError at load time
    - Test loadConfig with invalid global config file throws ValidationError at load time
    - Test saveConfig with invalid config throws ValidationError before writing file
    - Test loadConfig with valid project config succeeds
    - Test saveConfig with valid config writes file successfully
    - Run tests: should fail because loadConfig/saveConfig do not call validateConfig
  </test>
  <action>
    GREEN: Integrate validation into config.ts:
    - Call validateConfig(result) at end of loadConfig before returning
    - Call validateConfig(config) at start of saveConfig before writing file
    - Update existing config.test.ts if needed to use valid configs
    - Tests should now pass
  </action>
  <refactor>
    REFACTOR: Error context and testing:
    - Enhance ConfigValidationError to include source context (load vs save, project vs global path)
    - Add helper to wrap validation with file path context
    - Ensure all error messages are actionable and reference specific file when applicable
    - Update test fixtures to cover edge cases (empty fallbacks array, custom roles with invalid refs)
  </refactor>
  <verify>Run: cd packages/router && bun test - all config tests pass including validation integration</verify>
  <done>loadConfig and saveConfig call validateConfig, invalid configs are rejected early with clear errors listing all issues</done>
</task>
