{
	"name": "@draht/deploy-guardian",
	"version": "2026.3.1",
	"private": true,
	"description": "Deploy Guardian — pre-deployment checks, rollback automation, SST safety",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		},
		"./extension": {
			"types": "./src/extension.ts",
			"import": "./src/extension.ts"
		}
	},
	"scripts": {
		"build": "tsgo --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"vitest": "^3.2.4",
		"@draht/coding-agent": "workspace:*",
		"@types/node": "^22.10.5",
		"typescript": "^6.0.0-beta"
	}
}
