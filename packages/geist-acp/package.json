{
	"name": "@draht/geist-acp",
	"version": "2026.7.11",
	"private": true,
	"description": "ACP client + HarnessSession implementation — the only code that knows ACP wire shapes (geist spec §6)",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsgo --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@draht/geist-core": "workspace:*",
		"@draht/geist-protocol": "workspace:*"
	}
}
