{
	"name": "@draht/geist-core",
	"version": "2026.9.5-1",
	"private": true,
	"description": "geist harness-free product logic — sessions, projects, registry, git, variants, composer, ports (imports no @draht/* other than geist-protocol)",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@draht/geist-protocol": "workspace:*"
	},
	"devDependencies": {}
}
