{
	"name": "@draht/geist-picker",
	"version": "2026.7.11",
	"private": true,
	"description": "geist's in-page element picker — IIFE injected into target pages for element hover/highlight/crop (spec §10)",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsgo --noEmit",
		"test": "bun test"
	},
	"dependencies": {},
	"devDependencies": {}
}
