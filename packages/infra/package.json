{
	"name": "@draht/infra",
	"version": "2026.7.13-1",
	"private": true,
	"description": "SST v4 infrastructure for Draht — Lambda, API Gateway, DynamoDB",
	"type": "module",
	"scripts": {
		"dev": "sst dev",
		"build": "tsc --noEmit",
		"clean": "rm -rf dist .sst"
	},
	"dependencies": {
		"sst": "^4.2.1"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.145",
		"@types/node": "^22.10.5",
		"typescript": "7.0.2"
	}
}
