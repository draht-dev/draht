{
	"name": "@draht/rlm",
	"version": "2026.7.11",
	"private": true,
	"description": "Recursive Language Models — root-LLM-produces-code / REPL-executes / truncated-stdout scaffold",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsgo --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"vitest": "^3.2.4",
		"@types/node": "^22.10.5",
		"typescript": "^6.0.0-beta"
	}
}
