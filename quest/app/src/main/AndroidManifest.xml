<?xml version="1.0" encoding="utf-8"?>
<!--
  geist Quest 3 client — foundation skeleton (Phase 31).
  Meta Spatial SDK manifest entries (passthrough permission, headset feature flags,
  Spatial SDK activity metadata, etc.) land in Phase 32 (M0 — spike: panel + ray).
  See .planning/specs/geist-spec.md §7/§8/§16.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="geist"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
