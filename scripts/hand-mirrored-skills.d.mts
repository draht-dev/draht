/**
 * Hand-written type declarations for ./hand-mirrored-skills.mjs.
 *
 * Under Node16/NodeNext module resolution, a relative "./hand-mirrored-skills.mjs"
 * import specifier resolves its declarations from this ".d.mts" file. Keep in
 * sync with the exported signatures in the .mjs file.
 */

/**
 * Plugin skill directories hand-maintained as byte-identical pairs in both
 * packages instead of being generated from canonical skills/ — see the .mjs
 * for the admission criteria.
 */
export declare const HAND_MIRRORED_SKILL_DIRS: readonly string[];
